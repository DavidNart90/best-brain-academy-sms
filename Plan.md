# Best Brain Academy: Implementation Plan

**Owner:** Chief Engineer (the user)  
**Created:** 31 August 2026  
**Execution status:** Phase 5 Dashboards, Reports, and Performance is verified complete as of 9 September 2026. The Chief Engineer opened Phase 6 on 9 September 2026; the P6-01 route/database/rate-limit security slice and safe installable app shell, P6-L01 Library Books & Prospectus extension, P6-S01 salary cash workflow, P6-R01 role workspaces, and P6-F01 finance oversight/end-of-term billing extension are verified, while D-05 release-environment, recovery, and monitoring work remains active. E6-13 records a clean pre-push application/security/UI review and focused corrective pass; it is not P6-05 release acceptance.
**Active phase:** Phase 6 Client Data, Release, and Handover; P6-01 is in progress.
**Current implementation task:** Review and push the E6-13 pre-push candidate when ready, then resume the remaining D-05/P6-01 release-environment, recovery and monitoring decisions or begin the separately authorized P6-02 import-template work. Term 2 generation remains correctly blocked until its school-fee and Books & Prospectus rates are configured. The separate Chief Engineer decision on retaining or audit-reversing 15 September salary calculations remains pending; no salary payment or SSNIT remittance occurred, and August remains explicitly unposted.

## 1. How to Use This Plan

Read [AGENTS.md](AGENTS.md), [Project.md](Project.md), and [design.md](Project%20Files/design.md) at session startup, then this plan's current phase, dependencies, decisions, and handover. Read the [original project guide](Project%20Files/PROJECT_GUIDE_BEST_BRAIN_ACADEMY.md) for business rules and the [detailed design system](Project%20Files/design_system.md) plus [inspiration image](Project%20Files/design_inspiration.jpg) for visual work.

This document controls sequence and progress, not product scope. `Project.md` controls engineering standards; the original guide controls business rules; the design documents control appearance. This plan adds the requested Phase 0 foundation before the original guide's six phases. It does not authorize production changes, expand scope, or record unprovided approvals.

When implementation is requested, begin the next dependency-ready task and proceed within the authorized scope. Do not stop for routine permission on every task. Bring material scope, architectural, financial-policy, and release-risk decisions to the Chief Engineer with evidence and a recommendation. Do not skip a failed gate to make the schedule look complete.

### Task status and evidence

- **Not started:** No implementation claimed.
- **In progress:** Active work; record owner, files, and next action in the handover.
- **Blocked:** A specific dependency is unavailable; record what is needed and continue independent authorized work.
- **Verified complete:** Deliverable and applicable checks passed, with an evidence entry. Written code without verification stays in progress or blocked.

Each task table lists its direct dependencies; prerequisites also apply transitively. A phase is complete only when its exit-gate task is verified complete. Dependencies within a phase permit independent work, but do not authorize spawning agents automatically.

Keep IDs stable. Update this plan after meaningful progress and before ending an implementation session. Checks must be proportional to the change: do not rerun unrelated suites or create one-off harnesses merely to close a phase. Keep security, data-integrity, and financial invariants where applicable, and never mark a failed or skipped applicable check as passing. Evidence should be concise: task/date, material files or migration, applicable results, and remaining risk. Do not invent commit IDs or store secrets in evidence.

## 2. Phase Overview and Requirement Coverage

| Phase | Outcome | Entry dependency | Exit gate | Status |
| --- | --- | --- | --- | --- |
| 0 | Reproducible, protected application foundation and reference-based shell | Specifications available; environment decisions only block dependent tasks | P0-09 | Verified complete — E0-06 |
| 1 | Academic configuration, school settings, audit foundation, fast table pattern | P0-09 | P1-05 | Verified complete — E1-01 |
| 2 | Admissions, people records, enrollment history, administrator management | P1-05 | P2-06 | Verified complete — E2-01–E2-06; Chief Engineer accepted closure 2 September 2026 |
| 3 | Invoice, payment, receipt, daily expense, reversal, document, and outstanding-balance correctness | P2-06 plus staff-roster entry checkpoint | P3-06 | Verified complete — E3-13 |
| 4 | Audited monthly salary records, deductions, and approved operational-finance refinements | P3-06 | P4-04 | Verified complete — E4-01, E4-02 |
| 5 | Reconciled dashboards/reports, exports, and measured performance | P4-04 | P5-05 | Verified complete — E5-01/E5-02 |
| 6 | Validated client import, release acceptance, deployment, and handover | P5-05 | P6-08 | In progress — P6-01 security slice verified, E6-01 |

| Project requirement | Delivery coverage | Required evidence |
| --- | --- | --- |
| AUTH-01 | Phases 0, 2, 6 | Sign-in/session tests, explicit role assignment, denied access, privileged-account protection. |
| ACAD-01 | Phases 1–2 | Valid academic context, archive rules, preserved enrollment history. |
| PEOPLE-01 | Phase 2 | Admission/student/guardian/staff journeys, uniqueness, no automatic staff login. |
| FEE-01 | Phase 3 | Fee configuration, immutable invoice snapshots, correct historical reprints. |
| PAY-01 | Phase 3 | Exact amounts, atomic writes, concurrent requests, idempotency, reversals, unique receipts. |
| OPS-01 | Phases 3–4 | Phase 3 daily expenses and Phase 4 salary deductions use valid amounts, protected corrections, and void exclusion from totals. |
| REPORT-01 | Phase 5 | Reports and exports reconcile to authoritative records under the same filters. |
| ADMIN-01 | Phases 0–2, 6 | Restricted settings/admin operations, audit integrity, controlled provisioning. |
| IMPORT-01 | Phase 6 | Preview, validation, explicit confirmation, retry safety, reconciliation. |
| SEC-01 | Every phase | Server authorization, RLS, safe secrets/storage, security review and negative tests. |
| PERF-01 | Phases 1–4 | Bounded queries and pagination; Phase 5 performs representative load measurements and optimization. |
| QA-01 | Every phase | Relevant unit/integration/browser checks plus lint, typecheck, and production build. |

## 3. Gates Applied to Every Phase

### Engineering and design

Use modular features, thin routes, typed server-only data access, validated inputs, semantic tokens, and accessible components. Apply the required design guidance only when UI changes, run `react-doctor` only after React changes, and inspect only the affected journeys and responsive breakpoints. Documentation-only or backend-only work does not require browser/design checks.

### Database changes

For a database change, confirm the target, prepare one coherent versioned migration, apply it through MCP, regenerate types when contracts changed, and verify its constraints and RLS with representative roles. Run advisors or query diagnostics when the change or a failure gives a concrete reason; do not perform broad diagnostics for UI/documentation-only work.

Test migrations in the isolated target. Keep recovery notes for risky or data-changing migrations. No production reset, untracked DDL, deletion of posted records, or silent direct-connection fallback.

### Test and build gate

Run the smallest set that proves the changed behavior. For Phase 3, database and integration tests of the real financial rules are the primary acceptance evidence; broad end-to-end suites are not a per-task requirement:

```text
pnpm lint
pnpm typecheck
pnpm test                 # relevant domain, validation, and component behavior
pnpm test:db              # finance RPC, transaction, RLS, concurrency, and retry behavior
pnpm build
```

Outside finance work, add `pnpm test:db` only for database/Auth/RLS changes. For Phase 3 UI and document changes, run focused browser interaction, accessibility, responsive, and print checks for the affected screen; do not create or require a broad Playwright end-to-end suite. Use coverage checks at major integration/release gates, not after every task. Full cross-browser and performance/load work belongs in Phase 5/6 unless an earlier change directly affects those risks.

Do not generate long evidence for routine green checks. Record failures, material security/database results, and known limitations. An unavailable environment blocks only the affected deliverable; it does not require rerunning unrelated checks.

## 4. Phase 0: Production-Quality Foundation

**Objective:** Establish the approved stack, authentication, permission boundaries, and dashboard shell without business CRUD.  
**Requirements:** AUTH-01, ADMIN-01 foundation, SEC-01, PERF-01 foundation, QA-01.  
**Entry:** Read the specifications; identify D-01/D-02 dependencies without blocking offline scaffolding.

| Task | Depends on | Deliverable | Status |
| --- | --- | --- | --- |
| P0-01 | None | Inspect repository; scaffold Next.js App Router with `src/`, strict TypeScript, supported Node LTS, pnpm, and latest stable compatible Tailwind. Recheck versions, use v4 CSS/PostCSS configuration, pin dependencies, and preserve existing documents. | Verified complete — E0-01 |
| P0-02 | P0-01 | Configure ESLint, Prettier, typecheck, Vitest/Testing Library, Playwright, database-test harness, and CI commands. Create meaningful test scaffolding and isolated fixture conventions; do not use empty tests as green gates. | Verified complete — local, database and browser harnesses pass; CI definition reviewed, E0-02/E0-06 |
| P0-03 | P0-01; D-01 | Verify intended Supabase development/test targets via MCP. Configure browser/server clients, public versus secret environment variables, `.env.example`, and environment validation. | Verified complete — approved test-only project matches clients and harness; E0-04 |
| P0-04 | P0-03 | Create profiles linked to `auth.users`, roles, permissions, role-permission and user-role assignments, RLS/grants, and safe profile creation. Seed role definitions; document explicit initial administrator assignment without automatic escalation. | Verified complete — four migrations applied, SQL/RLS contracts and explicit test assignments verified, E0-04/E0-06 |
| P0-05 | P0-04 | Implement email/password login, logout, verified SSR session handling, protected routes, redirects, account-status enforcement, sign-in abuse controls, and safe auth errors. Disable public signup. | Verified complete — real login/refresh/logout/revocation, denied statuses, signup denial and hosted 429 verified, E0-06 |
| P0-06 | P0-05 | Implement server-only permission helpers, minimal typed data contracts, and permission-aware navigation. Cover direct endpoint access, not just hidden menu items. | Verified complete — real RLS actors, direct-write denial, metadata tampering and browser route denial pass, E0-06 |
| P0-07 | P0-01; P0-06 | Implement school-red tokens, accessible login, AppShell, sidebar, top bar, state components, Money formatting, and chart plus four-KPI dashboard with visibly labeled synthetic fixtures. | Verified complete — responsive/keyboard/axe and formatting checks pass, E0-02/E0-06 |
| P0-08 | P0-06; P0-07 | Add the full Phase 0 route shells from the brief, including expandable Admissions/Financials navigation, permission-denied states, loading/error/not-found handling, and clearly unavailable future actions. | Verified complete — 26 module/detail shells and direct-denial states pass, E0-02/E0-06 |
| P0-09 | P0-02; P0-08 | Verify foundation, update README setup/run/migration instructions, and record the first integrated quality/security/browser evidence. | Verified complete — all common gates plus real-provider checks pass, E0-06 |

**Affected areas:** `src/app/`, shared components, `features/auth/`, `lib/supabase/`, `lib/auth/`, `lib/permissions/`, generated types, environment/configuration files, CI, and auth/permission migrations. Establish `/login`, `/dashboard`, `/admissions`, `/admissions/new`, `/students`, `/classes`, `/staff`, the documented `/financials/*` routes, `/reports`, `/administrators`, and `/settings`.

**Exit evidence:** All common gates; successful and failed login, expiry/refresh/logout, tampered role denial, disabled account denial, no public signup, safe profile trigger, and direct unauthorized route/action access. Inspect desktop/tablet/mobile shell and assert `GHS 1,250.00` formatting. Verify no privileged values enter client bundles. Record initial bundle/network measurements; do not claim real table performance from demo fixtures.

**Recovery:** Rebuild only disposable test environments. Never reset a live Auth project or use a role seed to promote arbitrary accounts.  
**Excluded:** People CRUD, academic business CRUD, fee calculations, payments, receipts, expenses, reporting, and production deployment.

## 5. Phase 1: Academic Configuration and Shared Operations

**Objective:** Make school/academic configuration and audit behavior usable, with a bounded reusable table implementation.
**Requirements:** ACAD-01, ADMIN-01, SEC-01, PERF-01, QA-01.  
**Entry:** P0-09 verified complete.

| Task | Depends on | Deliverable | Status |
| --- | --- | --- | --- |
| P1-01 | P0-09 | Create academic-year, term, class, school-setting, and protected audit structures with validation, referential integrity, suitable indexes, and permission policies. | Verified complete — E1-01 |
| P1-02 | P1-01 | Implement authorized academic years/terms/classes and school settings, valid current context, archive behavior, and audited configuration changes. Use configurable values rather than real-school assumptions. | Verified complete — E1-01 |
| P1-03 | P1-02 | Build reusable table/query contracts: bounded server pagination, stable sorting, URL filters, debounced search, typed errors, and consistent empty/loading states. Apply first to classes/configuration. | Verified complete — E1-01 |
| P1-04 | P1-03; D-02 | Record the school operating profile and confirm the reusable list uses bounded pagination and no per-row query waterfall. Defer load testing to Phase 5 when representative finance data exists. | Verified complete — operating profile supplied; bounded class query verified in E1-01 |
| P1-05 | P1-04 | Verify academic/context/archive rules, audit restrictions, denied writes, and the affected configuration/table journeys. | Verified complete — E1-01 |

**Affected areas:** `features/academics/`, `features/settings/`, shared table controls, `/classes`, academic/school settings, audit helpers, and academic/settings/audit migrations.

**Exit evidence:** Invalid term/year relationships and duplicate identifiers are rejected; current context, archive behavior, audit immutability, permission denial, URL filters, stable sorting, and 25-row pagination work. Representative load testing is intentionally deferred to P5-04.

**Recovery:** Prefer additive schema and setting changes; preserve referenced records and audit entries.  
**Excluded:** Admissions, student/staff management, and financial transactions.

## 6. Phase 2: People and Administrator Workflows

**Objective:** Support operational admissions, student/guardian/staff records, historical enrollment, and controlled user administration.  
**Requirements:** PEOPLE-01, ACAD-01, AUTH-01, ADMIN-01, SEC-01, PERF-01, QA-01.  
**Entry:** P1-05 verified complete.

| Task | Depends on | Deliverable | Status |
| --- | --- | --- | --- |
| P2-01 | P1-05 | Create students, guardians, relationships, enrollments, and class/location assignments with unique identifiers, history preservation, indexing, and least-privilege policies. Staff remains a separate P2-04 task. | Verified complete — E2-01/E2-02 |
| P2-02 | P2-01 | Implement admission validation and transactional student/guardian/enrollment creation, records search, duplicate handling, explicit failure states, and the approved student Excel onboarding workflow. Admission works without an invoice. | Verified complete — E2-01/E2-02 |
| P2-03 | P2-02 | Implement student profiles, guardian links, enrollment changes/history, and secure photo upload/access. Keep finance tabs explicitly unavailable until Phase 3. | Verified complete — E2-03 |
| P2-04 | P2-01 | Implement staff profiles, assignments, search, archive rules, and audit context; a staff record creates no login account. | Verified complete — E2-04 |
| P2-05 | P1-05; D-03 | Implement authorized administrator password provisioning, forced first-login password replacement, role assignment, account disablement, and fresh permission enforcement. Prevent self-escalation and unsafe loss of administrative access. | Verified complete — E2-05/E2-05A |
| P2-06 | P2-03; P2-04; P2-05 | Verify admission rollback/duplicates, student and staff journeys, private photos, administrator role changes, and denied access. | Verified complete — prior live verifications in E2-01–E2-05 plus E2-06A/B; Chief Engineer accepted closure, E2-06C |

**Affected areas:** `features/admissions/`, `features/students/`, `features/staff/`, admin/auth modules, corresponding list/detail routes, Storage policies, and people/enrollment migrations.

**Exit evidence:** Duplicate or failed admission leaves no partial records; enrollment history remains readable; staff records do not create login accounts; unauthorized IDs/photos are denied; disabled or de-privileged accounts lose access; student search remains bounded for the expected 200 students.

**Recovery:** Preserve historical links; repair imports/records through audited permitted operations. Do not delete people records with financial/history dependencies.  
**Excluded:** Live finance, broader release/client migration beyond the approved student onboarding template, payroll, attendance, examinations, or parent portals.

## 7. Phase 3: Finance Core

**Objective:** Establish trustworthy invoicing, daily receipts, daily expenses, official financial documents, and cashflow controls before reporting.

**Requirements:** FEE-01, PAY-01, OPS-01 (daily expenses), SEC-01, PERF-01, QA-01.

**Entry:** P2-06 is verified complete and Phase 3 is authorized. On 3 September 2026 the Chief Engineer authorized starting P3-01 schema work in parallel with the remaining STAFF-01 roster facts (nursery aliases, Term 1 assignment dates, head/class teachers), since finance schema does not depend on those specific facts; STAFF-01 itself must still close before Phase 3 is considered complete. Unknown staff contact details stay absent; no name, employment date, or head-class-teacher designation may be fabricated. D-04 gaps use the configurable proposals in [Financial Structure.md](Project%20Files/Financial%20Structure.md) until the Chief Engineer reviews them.

**STAFF-01 prerequisite:** Complete. Migration 20260902185750 adds optional unknown details, preserved full names, server-numbered IDs, class-subject pairs, separate headship, request replay and atomic audit. All 18 source staff were saved through authenticated Add Staff forms and reconciled privately (including corrections 004/006): 14 teaching, four non-teaching, 18 requests and 18 staff audit entries; next number 019. Twelve distinct supplied headships now cover 12 distinct classes, including Nursery 1/2, in 2026/2027 Term 1 from 8 September 2026; unspecified specialist classes remain unset. A two-session rollback probe verified counter-lock serialization with no residue. The private generated workbook was incompatible with ExcelJS and was not imported; do not retry it after manual onboarding. See E3-00C and [Phase 3 closure evidence](docs/evidence/phase-3/phase-3-closure-verification.md).

**Finance authority:** [Financial Structure.md](Project%20Files/Financial%20Structure.md) defines the supplied fee matrix, daily receipt categories, calculations, controls, phase mapping, and beta decisions.

| Task | Depends on | Deliverable | Status |
| --- | --- | --- | --- |
| P3-01 | P2-06; STAFF-01; D-04 configurable proposals | Design and apply a coherent, reviewable finance migration set covering fee components and effective-dated structures; immutable invoice lines/documents; payments; feeding/admission/miscellaneous receipts; simple daily expenses; payment methods and categories; reversals; unique server-side numbering; idempotency/request fingerprints; exact `NUMERIC(14,2)` money; atomic audit; grants; closed direct writes; and RLS on every table. Regenerate types and run focused database verification plus security/performance advisors through the authorized test-only Supabase MCP workflow. | Verified complete — E3-02–E3-05, E3-13 |
| P3-02 | P3-01 | Implement authorized financial settings for effective-dated base class fees, location/transport charges, feeding/admission amounts, income/expense categories, payment methods, numbering, and required/optional components. Install the documented configurable defaults on the test project without hard-coded application amounts and keep them visibly reviewable. | Verified complete — E3-06, E3-13 |
| P3-03 | P3-02 | Implement invoice generation/cancellation with immutable line and identity snapshots, unique numbering, invoice views, and the first reviewed official document set: A4 invoice plus compact receipt/expense-voucher designs that remain legible in grayscale and preserve historical snapshots. | Verified complete — E3-07, E3-08, E3-10, E3-13 |
| P3-04 | P3-03 | Implement transactional RPCs for school-fee payment plus balance plus receipt plus audit, feeding/admission/miscellaneous receipts, and simple daily expenses. Use decimal-string transport, row-lock serialization, server numbering, duplicate prevention, and idempotent retry/unknown-outcome recovery. | Verified complete — E3-09, E3-13 |
| P3-05 | P3-04 | Implement authorized payment/receipt/expense reversals and the combined daily cashflow workspace. Show receipt, expense, and net totals by business date; add receipt/voucher list, view, reprint/PDF, outstanding balances, and student account history with bounded queries and clear reversal state. Unlock only the backed, verified student finance sections. | Verified complete — E3-09–E3-13 |
| P3-06 | P3-05 | Verify the actual finance business logic: exact decimals, effective dates, immutable snapshots, full/partial payments, row-lock concurrency, idempotent replay and changed-payload rejection, numbering uniqueness, duplicate feeding/admission protection, expense validation, rollback of every atomic write, reversal permissions, reconciliation, RLS/direct-write denial, and document reprint/VOID behavior. Run focused affected-screen/browser/print checks, not a broad E2E suite. | Verified complete — E3-13 |

### Phase 3 execution breakdown (internal checklist, IDs are working notes, not new plan rows)

Each P3-0X task above is implemented as these smaller, independently verifiable steps. This does not change the stable P3-01…P3-06 IDs, dependencies, or exit gates.

- **P3-01 (schema):** (a) reference/config tables — fee components, payment methods, expense categories, misc-income categories; (b) invoice + immutable invoice-line tables; (c) payment + receipt + idempotency/request-fingerprint tables; (d) feeding/admission/misc receipt tables; (e) expense + voucher-numbering tables; (f) reversal/void columns + audit trigger wiring; (g) RLS/grants/closed-direct-writes on every table; (h) regenerate types, run advisors, verify constraints with representative roles.
- **P3-02 (settings):** base class fee config (effective-dated, per class) → location/transport charge config (extends existing Transport locations settings) → feeding/admission fee amount settings → payment methods + expense categories + misc-income categories CRUD → numbering-prefix display.
- **P3-03 (invoicing/documents):** invoice generation RPC (bulk per-term + single-student) → invoice cancellation RPC → invoice list/detail views (unlocks the student finance tab) → A4 invoice document → compact A5 receipt/voucher shell (reused by P3-04/05).
- **P3-04 (transactional RPCs):** school-fee payment RPC → feeding-receipt RPC → admission-receipt RPC → misc-receipt RPC → daily-expense RPC → shared numbering helper.
- **P3-05 (reversals/workspace):** payment/receipt reversal RPC → expense void RPC → daily cashflow entry screen → daily summary (receipts/expenses/net) → receipt/voucher list + reprint → outstanding-balances list + student account history.
- **P3-06 (verification):** DB/RPC focused tests (decimals, snapshots, concurrency, idempotency, numbering, duplicate prevention, rollback, reversal permissions, RLS) → focused browser/print checks on affected screens/documents only → evidence + closure request.

**Affected areas:** `features/finance/`, student finance tabs, daily cashflow workspace, invoice/payment/receipt/expense/outstanding routes, document components, RPCs, constraints, grants/RLS, and versioned finance migrations. Exact schema details are reviewed at P3-01; do not treat this list as pre-applied SQL.

**Exit evidence:** Full/partial/multiple payments; zero/negative/overpayment rejection; two writers competing for the same balance; duplicate request returns the original result; changed payload with the same key is rejected; lost response does not duplicate records. Inject a transactional failure and confirm payment/balance/receipt/audit roll back together. Verify receipt uniqueness, prohibited posted edits/deletes, reversal history, exact serialization, and changed settings not altering old documents. A PDF failure must not undo or repeat a committed payment. Measure locking and RPC latency with actual permissions.

**Recovery:** Prefer forward fixes and audited reversals over destructive rollback. Verify recovery on test data before schema promotion.  
**Excluded:** Salary deductions, live aggregate dashboards/reporting beyond the selected-day cashflow summary, credits unless explicitly approved, payment gateways, and any full payroll engine. Phase 5 cannot start while P3-06 is unverified.

## 8. Phase 4: Operational Finance

**Objective:** Add traceable monthly gross salary, approved deductions, final net position, and any separately approved operational-finance refinements without expanding into a tax/pension/payslip engine or duplicating the Phase 3 daily expense ledger.

**Requirements:** OPS-01, ADMIN-01, SEC-01, PERF-01, QA-01.  
**Entry:** P3-06 verified complete.

| Task | Depends on | Deliverable | Status |
| --- | --- | --- | --- |
| P4-01 | P3-06 | Create configurable staff salaries and salary-deduction types plus monthly staff salary/deduction/reversal records with constraints, indexes, policies, snapshots, and atomic audit. | Verified complete — E4-01, E4-02 |
| P4-02 | P4-01 | Implement staff/month gross-salary and deduction entry, positive-amount validation, unique references, safe retry behavior, filters, and controlled corrections; no tax or payslip engine. | Verified complete — E4-01 |
| P4-03 | P4-02 | Integrate active monthly deductions into gross-minus-deductions net position and bounded staff salary history; keep historical calculation inputs immutable. | Verified complete — E4-01 |
| P4-04 | P4-03 | Verify permissions, invalid amounts, retry safety, protected posted records, reversal behavior, replacement after reversal, monthly calculations, audit, and bounded lists. | Verified complete — E4-01 |

**Affected areas:** Salary-register modules, staff salary history, financial settings, `/financials/salary-deductions`, and operational-finance migrations. No attachment/storage scope was approved or added.

**Exit evidence:** Staff salary rates are effective-dated and can be added, changed, or ended without deleting history; posted salary records and deductions cannot be edited/deleted through an alternate API; reversals retain references/reason/actor/time; deduction corrections refresh active totals and preserve history; retry keys return the original result; invalid values and unauthorized posting fail; lists are bounded. E4-01 and E4-02 record the focused database and authenticated browser checks.

**Verified scope note:** The supplied 5.5% SSNIT employee contribution is an effective-dated, configurable automatic deduction beginning August 2026. Full PAYE, employer pension/liability accounting, automated payroll runs, approvals, bank files, and payslips remain outside Phase 4.

**Recovery:** Correct posted expenses through a traceable void/replacement, never by erasing history.  
**Excluded:** PAYE, employer SSNIT, pension-liability accounting, payslips, automated payroll runs, and formal financial reporting.

## 9. Phase 5: Dashboards, Reports, and Performance

**Objective:** Replace demo summaries with reconciled authorized data and deliver useful, fast reporting.  
**Requirements:** REPORT-01, PERF-01, SEC-01, QA-01.  
**Entry:** P4-04 verified complete, including the finance gate transitively.

| Task | Depends on | Deliverable | Status |
| --- | --- | --- | --- |
| P5-01 | P4-04 | Build authorized daily, weekly, monthly, and term aggregates with explicit date semantics. Reconcile school-fee, feeding, admission, miscellaneous, expense, deduction, and outstanding figures to source records. | Verified complete — E5-01 |
| P5-02 | P5-01 | Connect reference-layout dashboard, financial overview, four KPIs, charts, and recent collections to authorized database records; remove demo indicators only where the data connection is implemented and verified. | Verified complete — E5-01 |
| P5-03 | P5-01 | Implement required financial/admin reports, student statements, and bounded print/PDF/CSV/Excel exports with matching filters and formula-injection protection. | Verified complete — E5-01 |
| P5-04 | P5-02; P5-03; D-02 | Test the actual high-value dashboard, list, payment, and export paths at the agreed school volumes and 10-user peak. Fix evidenced bottlenecks; avoid a separate general-purpose benchmark framework. | Verified complete — E5-01/E5-02 |
| P5-05 | P5-04 | Verify report reconciliation, access boundaries, fresh post-write balances, supported-browser visuals, and the measured high-value paths. | Verified complete — E5-01/E5-02 |

**Affected areas:** `features/reports/`, financial/dashboard queries, `/dashboard`, `/financials`, `/reports`, export modules, aggregate/view/index migrations, and telemetry.

**Exit evidence:** Summaries and exported rows reconcile to invoices/payments/reversals/expenses under identical filters; void records are excluded correctly; management cannot mutate records or export unauthorized fields. Test empty periods, date boundaries, large exports, permission changes, and cache isolation. Inspect official documents in grayscale and at print size.

Use the supplied operating profile: approximately 200 students, 5,000–10,000 finance records per academic term, 10 devices, and a 10-user peak. Test Chrome/Edge-compatible Chromium on desktop plus responsive tablet/mobile layouts. Use one ordinary connection profile and one constrained profile until the school connection is measured.

**Recovery:** Keep aggregates reconstructible from authoritative records; remove a faulty derived cache/view without rewriting the ledger.  
**Excluded:** Client-data import, production rollout, invented growth metrics, and unrelated analytics modules.

## 10. Phase 6: Client Data, Release, and Handover

**Objective:** Deliver a recoverable, verified system on confirmed school configuration.  
**Requirements:** IMPORT-01, REPORT-01, AUTH-01, ADMIN-01, SEC-01, PERF-01, QA-01.  
**Entry:** P5-05 verified complete. Infrastructure, client data, and release decisions apply only to their dependent tasks.

| Task | Depends on | Deliverable | Status |
| --- | --- | --- | --- |
| P6-01 | P5-05; D-05 | Establish release environments, domain/configuration plan, security headers/rate limits review, alerts, database plus Storage backups, recovery objectives, and a tested restore runbook. | In progress — security headers, route/access audit, request limits, app rate limits, RLS/function/storage review verified in E6-01; online-only PWA/update/navigation/search shell verified in E6-11; D-05 environment/recovery/monitoring work remains |
| P6-L01 | P5-05; D-12 | Add configurable per-term Books & Prospectus rates and a separate Library charge/collection balance, surface the separate charge on student invoices, add Library financial KPIs, and authorize a dedicated Librarian / Book Keeper role. | Verified complete — E6-02 |
| P6-S01 | P4-04; P5-05; D-13 | Add retry-safe partial/full employee salary payments and SSNIT employee-contribution remittances against posted salary calculations, plus safe one-click monthly posting and exact outstanding-net dispatch, with reversals and one linked outgoing-expense entry per employee cash transaction. | Verified complete — E6-05, E6-06 |
| P6-R01 | P6-L01; P6-S01; D-14 | Align navigation, dashboards, reports, filtered printing, server authorization, and database grants to the approved four operational roles plus the Super Administrator system role. | Verified complete — access foundation E6-07; Administrator filters/print E6-08; role dashboards E6-09; role Settings, five-role live journeys, revised Board read access/dashboard and test-account cleanup E6-10 |
| P6-F01 | P6-R01; P3-03; P5-01; D-15 | Replace the fee-centric finance landing with operational oversight and add immutable, bounded end-of-term next-term invoice preparation, generation and printing. | Verified complete — E6-12; Term 2 remains unconfigured and no real invoice was generated |
| P6-02 | P5-05 | Implement required CSV/Excel import templates, preview, validation, duplicate detection, confirmation, bounded batches, retry-safe behavior, and import outcome records. | Not started |
| P6-03 | P6-01; P6-02; D-06 | Validate confirmed school files and configuration, rehearse imports in staging, reconcile source counts/amounts, and fix row-level issues without silently discarding data. | Not started |
| P6-04 | P6-03; D-04 | Run school acceptance scenarios, inspect invoices/receipts with confirmed samples, verify actual role matrix/device baseline, and produce a release evidence report. | Not started |
| P6-05 | P6-04 | Run the full release suite once on the candidate build, review applicable security findings, confirm recovery, and document residual risks for the Chief Engineer. | Not started |
| P6-06 | P6-05; D-07 | After release acceptance, apply reviewed production migrations through MCP, set verified configuration, provision the initial administrator explicitly, and perform authorized final import with reconciliation. | Not started |
| P6-07 | P6-06 | Deploy the accepted build, verify HTTPS/domain/session behavior, run non-destructive production smoke checks, confirm no test data, and monitor launch errors. | Not started |
| P6-08 | P6-07 | Deliver staff training, operating/recovery documentation, access ownership, support responsibilities, and the Chief Engineer's handover acceptance. | Not started |

**Affected areas:** Import modules/templates, deployment/CI configuration, operational documentation, finalized settings/roles, release migrations, production bootstrap, and private storage/backup procedures.

**Exit evidence:** Trial import matches source records and configured fee values; malformed/duplicate rows are explainable; retries do not duplicate records; explicit import confirmation is enforced. Demonstrate database and uploaded-file recovery. Verify privileged MFA or an explicitly accepted alternative, no test accounts/data in production, and no public signup. Record the accepted release/version, migration identities, smoke results, monitoring owner, and handover artifacts. Keep real records out of screenshots and logs.

**Recovery:** Use the rehearsed application rollback or compatible forward migration; do not restore production blindly after new payments have been collected. Pause affected writes and obtain the required decision before a recovery that could discard financial history.  
**Excluded:** New product scope, destructive live testing, automatic production resets, and public release without acceptance.

## 11. Decisions and External Dependencies

These are unresolved planning inputs, not requests to answer everything now. Resolve each by its listed task; progress on independent work can continue.

| ID | Decision/input | Needed by | Owner | Status |
| --- | --- | --- | --- | --- |
| D-01 | Correct school Supabase project, development/test separation, MCP connection, and required environment access. | P0-03 | Chief Engineer supplies context; agent verifies target safely | Resolved for testing, 2026-08-31 — Chief Engineer designated `cefwopisbgfctzdloequ` as the test-only project on the Free plan; no separate paid branch required. Production conversion remains subject to D-07. |
| D-02 | School browsers/devices, typical network, expected volumes/concurrency, and acceptance of measured performance/coverage budgets. | P1-04, revisited P5-04 | Chief Engineer with agent benchmark evidence | Resolved for planning, 2026-09-01 — 10 devices; about 200 students; 5–10 concurrent users; Chrome/Edge; desktop-first with tablet/mobile responsiveness; 5,000–10,000 finance records per academic term (confirm if “each team” meant something else). Exact school network speed is unmeasured, so P5 uses ordinary and constrained profiles and P6 validates on site. |
| D-03 | Administrator permission matrix and privileged-account controls, including protection against accidental loss of admin access. | P2-05 | Chief Engineer | Revised and resolved for implementation, 2026-09-01 — use password-only login with no OTP/MFA. Only SUPER_ADMIN receives `administrators.manage` and `settings.manage`; ADMINISTRATOR receives all other current permissions. New accounts use an administrator-set temporary password and must replace it at first sign-in. Fresh permission/session checks, one explicit role, no self-role/status changes, and final-active-Super-Administrator protection remain mandatory. Release acceptance remains under D-07. |
| D-04 | Fee/income/expense/deduction rules, numbering, financial date/time semantics, and invoice/receipt samples. Keep credits disabled unless approved; use clearly provisional samples until final confirmation. | P3-01 for configurable proposals; P6-04 for final production approval | Chief Engineer/client, with configurable agent proposals | Revised and partially resolved, 2026-09-10 — [Financial Structure.md](Project%20Files/Financial%20Structure.md) includes the supplied notebook's daily/weekly cashflow pattern, simple daily expense entry, Phase 3 official document design, and configurable proposals for payment methods, expense categories, yearly document numbering with permitted gaps, A4 invoices, and A5 receipts/vouchers. D-13 resolves salary calculation, employee-payment, SSNIT-remittance, and reporting treatment. Discounts/credits/refunds, opening balances, date-close rules, official category approval, and final document/printer samples remain open. |
| D-05 | Hosting/domain/region, data retention, recovery objectives, backup coverage/cost, monitoring ownership. | P6-01; revisit earlier if environment choice requires it | Chief Engineer | Open |
| D-06 | Confirmed student/staff/admin files, classes, terms, fees, and categories; treatment of any existing balances must be specified rather than fabricated. | STAFF-01 before P3-01; remaining production data by P6-03 | Chief Engineer/client | Revised and partially resolved, 2026-09-09 — the 18 known-details staff are saved and privately reconciled (E3-00C); 12 supplied headships use the confirmed Nursery 1/2 aliases and 2026/2027 Term 1 context from 8 September 2026 (E3-13). The two specialists without supplied classes remain unassigned. Unknown contacts/employment details and any additional teaching pairs must remain absent until supplied. STAFF-01 is complete; student/admin production files, remaining categories, and opening-balance treatment remain open for P6. |
| D-07 | Acceptance of release evidence and explicitly documented residual risks; authorization for production promotion/import. | P6-06 | Chief Engineer | Open |
| D-08 | Phase 3 verification should prioritize actual financial business logic rather than broad end-to-end automation. | P3-01–P3-06 | Chief Engineer | Resolved, 2026-09-02 — require focused domain, database/RPC, transaction, RLS, concurrency, idempotency, reversal, and reconciliation tests. Use focused browser/accessibility/print checks only for affected UI/documents; a broad Playwright E2E suite is not a Phase 3 task gate. |
| D-09 | Staff IDs, incomplete details, teaching subjects/classes, and class leadership. | STAFF-01 | Chief Engineer | Resolved for STAFF-01, 2026-09-09 — use `BBS-Staff-001` onward; Class 1–6 means Basic 1–6, Class 7 means JHS 1, Class 8 means JHS 2, and Lower/Upper Nursery use Nursery 1/2. Twelve supplied class links are stored as separate head-teacher appointments in the confirmed academic context; the two specialists without supplied classes remain unassigned. A teacher may still teach several class–subject pairs, but unlisted pairs must not be inferred. See [staff requirements](docs/requirements/staff-onboarding.md) and E3-13. |
| D-10 | Phase 3 execution-shaping choices: invoice generation trigger, overpayment/credit policy, whether to install Financial Structure.md §16 numbering/payment-method/expense-category proposals verbatim, and fee-configuration granularity. | P3-01, P3-03 | Chief Engineer | Confirmed, 2026-09-03 — the Chief Engineer chose the agent's recommended STAFF-01 handling (start P3-01 schema now, resolve remaining roster facts separately) and confirmed (b) reject overpayment/no credits and (c) install the §16 numbering/payment-method/expense-category defaults verbatim. The Chief Engineer further confirmed that base class fees and location/transport charges are effective-dated **per academic year and term** (not permanent constants), starting with the supplied 2026/2027 Term 1 figures, and that this generic per-term configurability extends to every fee component that appears on an invoice, present or future. (a) invoice-generation trigger (bulk + single-student) and (d) per-class fee granularity remain the agent's proposed defaults, unchallenged. Applied in migration `20260903130000_finance_reference_configuration`, E3-02. |
| D-11 | Phase 4 salary-register scope, deduction rules, and verification breadth. | P4-01–P4-04 | Chief Engineer | Resolved for implementation and roster configuration, 2026-09-09 — the supplied August 2026 structure authorizes effective-dated staff salary settings, monthly gross salary records with a configurable 5.5% SSNIT employee deduction and optional fixed/percentage deductions, historical snapshots, and gross-minus-deductions net position. It explicitly excludes a full payroll/tax/pension/payslip engine. The Chief Engineer confirmed the unmatched schedule entry is a cook and confirmed the roster-only staff member is paid outside the register. E4-01/E4-02 use one focused rollback-only salary SQL file plus affected-screen browser inspection and standard quality gates; no broad suite was added. The roster is reconciled, but the Chief Engineer explicitly kept the August batch unposted. |
| D-12 | Books & Prospectus charging, class coverage, accounting separation, Library balance, and operating role. | P6-L01 | Chief Engineer | Resolved, 2026-09-10 — Books & Prospectus is one configurable combined fee per academic term and class. It is billed separately on the same student invoice, excluded from school-fee totals and accounting activity, and maintained as a separate Library balance where Net means Expected minus Paid. Nursery 1 and Nursery 2 remain unconfigured because their amounts are unconfirmed/unknown; JHS 1 is B7 at GHS 924.00, JHS 2 is B8 at GHS 924.00, and JHS 3 is explicitly not charged. A Librarian / Book Keeper may generate charges and record/reverse Library collections, while rate configuration remains administrator-only. |
| D-13 | Meaning of salary posting and payroll-deduction treatment in financial reports. | P4-03, P5-01, P6-S01 | Chief Engineer | Resolved and implemented, 2026-09-10 — posting a salary creates an auditable monthly salary calculation and does not record payment to the employee. GHS 27.50 on a GHS 500.00 calculation is specifically the automatic 5.5% SSNIT employee contribution. Gross less configured deductions is the salary-register net pay. Employee payments apply separately to net pay and SSNIT remittances apply separately to the employee contribution; each supports partial/full status, exact outstanding validation, retry safety, and reversal. Post all creates only missing monthly calculations; dispatch all records every posted employee's exact remaining net pay atomically and leaves SSNIT separate. Only active outgoing cash records reduce operating net and recorded cash position. Employer contribution, PAYE, payroll liabilities, approval runs, bank files, and payslips remain excluded. E6-05 and E6-06 record the proof. |
| D-14 | Final staff workspace responsibilities and least-privilege visibility for Administrator, Accountant, Board Member, Librarian, and Super Administrator. | P6-R01; P6-04 | Chief Engineer | Revised and implemented, 2026-09-15 — use four operational roles plus the Super Administrator system role. Accountants may view but not edit Student and Staff personal records; they operate all Financials, salary workflows, financial reports, and Financial Settings. Administrators operate Admissions and Student records, may view Teachers/Staff and required class context, and receive a frequently used outstanding-fees table with student-name, class, academic-term filters and filter-faithful printing; they may not record or reverse payments. Board Members receive cross-school read-only access to every Administrator, Accountant and Librarian operational workspace, but no create/edit/import, transaction, Library collection, configuration, administrator, audit or system controls; their dashboard reports school revenue, expenses, fees expected, net revenue, fees outstanding and fees paid by month, academic term or academic cycle with charts. Librarians retain the D-12 Library collection workspace. Every role has its own role-aware Settings landing page. Super Administrators can access every operational workspace plus administrator, configuration, audit, and system-health views. Preserve server/database enforcement; navigation hiding is not authorization. Present the existing `MANAGEMENT` role as Board Member without an unnecessary stored-code rename. |
| D-15 | Finance oversight composition and end-of-term next-term invoice behavior. | P6-F01; P6-04 | Chief Engineer | Resolved and implemented, 2026-09-15 — the finance landing and Accountant dashboard show gross receipts, expenses, operating net, recorded cash position, fee recovery, collection sources, expense categories and recent collections by month, academic term or academic cycle. End-of-term invoices are real next-term school-fee invoices with immutable target fee lines, a separate Books & Prospectus planning amount, the source-term outstanding balance, and an optional locked note to parents. Generation is retry-safe and cursor-bounded to 50 students per request; printing is class- or school-scoped and paginated at 25 documents. Accountant, Administrator and Super Administrator may configure/generate; Board Member remains review/print-only under D-14. No rate or promotion is inferred: same-year terms carry the current enrollment context; a new academic cycle requires a target-term enrollment. |

Resolve decisions with a dated rationale and reference, not by merely changing `Open` to `Resolved`. Requirements already established in `Project.md` need no repeated approval; decisions capture missing specifics and material exceptions. Do not set delivery dates until dependencies and effort estimates are grounded. Applicable security and financial-integrity checks cannot be traded away for schedule.

## 12. Progress Ledger and Session Handover

### Baseline

This plan contains implementation tasks only; all begin as **Not started**. Existing documents are planning inputs, not completed application phases. There is no `package.json`, application, migration set, or configured test runner at plan creation. The earlier pnpm command attempts failed because the manifest was absent; do not repeat those failures as supposed test coverage.

### Evidence log

Evidence from 31 August 2026 (owner: Codex; no commit created):

| ID / tasks | Change and checks | Limits / next step |
| --- | --- | --- |
| E0-01 / P0-01 | Pinned Node 24.20.0, pnpm 10.33.0, Next 16.3.3, React 19.2.8, Tailwind/PostCSS 4.3.3, TS 5.9.3; strict modular `src/` scaffold and lockfile. Frozen install, lint, types and production build exit 0. Original project references retained. | ESLint 9.39.5 remains for Next compatibility after ESLint 10 plugin failure; revisit upstream. Runtime setup and versions in README and decisions. |
| E0-02 / P0-02, P0-05–P0-08 local scope | 25 unit/component tests pass; scoped coverage 96.66% lines / 93.10% branches. Production-mode browser run: 21/21 pass across Chromium desktop/tablet/mobile in 51.5 s, including axe, filters, keyboard scrolling, focus return, all documented shells, status/grant changes, direct API denial and service-failure handling. School-red screenshots inspected against reference; GPT Taste dashboard overrides applied. | Auth double is synthetic and loopback only; no claim of real RLS, provider rate limiting or token-expiry refresh. Browser evidence, performance scope and review findings in [Phase 0 evidence](docs/evidence/phase-0/verification.md). |
| E0-03 / P0-03, P0-04, P0-09 — historical, before D-01 resolution | `.env.local` matched `https://cefwopisbgfctzdloequ.supabase.co`; initial MCP inspection found public tables `[]`, migrations `[]`, branches `[]`. Auth session column types confirmed. Two existing `rls_auto_enable` grant warnings; performance advisors empty. Migrations and 14 pgTAP assertions were then prepared but unapplied. Database/live browser commands exited 1 for missing configuration. | Historical baseline only, superseded by E0-04/E0-05 below. No writes had occurred at that point. |
| E0-04 / P0-03, P0-04 | Supersedes E0-03's environment/migration blockers. Chief Engineer authorized existing Free-plan project as test-only. MCP applied `20260831200632_foundation_access`, `20260831200637_restrict_existing_rls_trigger_grants`, `20260831200843_verify_foundation_access_contracts`; local filenames match history. Five tables have RLS. Executable SQL tests used authenticated/anon roles for isolation, metadata non-escalation, super/management/pending/disabled access, denied writes, changed grants/status and invalid/revoked sessions. All transient fixtures rolled back. Types regenerated from MCP. | Security advisors empty; two INFO unused foreign-key indexes retained without workload. Counts: zero Auth users/sessions/profiles/user-role assignments; four roles, nine permissions, 21 role-permission definitions. SQL checks are not Auth-issued JWT tests; separate pgTAP file not run. [Backend evidence](docs/evidence/phase-0/backend-verification.md). |
| E0-05 / P0-05, P0-09 — historical, before actor provisioning | Signed-in hosted dashboard: disabled/saved public signup; anonymous sign-ins/manual linking off, email confirmation on, other providers disabled. Four live preflight tests pass: anonymous table/RPC denial, private schema unexposed, signup disabled with actual rejected attempt, invalid login. Existing rate limits and localhost origin inspected. | At that point, both real-provider commands exited 1 because synthetic actors were absent. Superseded by E0-06. |
| E0-06 / P0-02, P0-04–P0-09 | On 1 September 2026, three auto-confirmed synthetic Auth users were created with strong generated passwords stored only in ignored `.env.test.local`. MCP applied `20260901100905_configure_phase0_test_actors`, resolving fixed test emails without storing generated UUIDs or passwords. Final states: allowed active/SUPER_ADMIN, denied pending/unassigned, disabled/SUPER_ADMIN; zero sessions after tests. `pnpm test:db` passed 8/8 including real login, RLS, refresh, metadata tampering, direct-write denial and post-logout revocation. `pnpm test:e2e:auth` passed 3/3 allowed/pending/disabled journeys. Controlled invalid login reached hosted HTTP 429 at attempt 34. Common gates passed: lint, format, typecheck, 25 unit tests, coverage 96.66% lines / 93.10% branches, build, client-boundary scan and 21/21 synthetic browser tests across desktop/tablet/mobile in 47.9 s; final real-project rebuild passed. | Supabase security advisor reports only `auth_leaked_password_protection`; official docs say this protection requires Pro, so it is recorded as a production-release limitation of the authorized Free test plan. Two INFO unused FK indexes remain pending workload. The standalone 14-assertion pgTAP file and hosted GitHub workflow were not run; executable migration SQL and every required local gate did run. No business data, deployment or Phase 1 work. [Backend evidence](docs/evidence/phase-0/backend-verification.md). |
| E1-01 / P1-01–P1-05 | On 1 September 2026, the Chief Engineer supplied the 2026/2027 academic, class, location, fee-reference, and operating-profile requirements. MCP applied `20260901111519_academic_configuration`; the local migration matches. Six RLS tables hold one year, three terms, 13 classes, five locations, one school-settings row and 23 immutable seed audit records. Term 1 starts 8 September; its 7 December end is disclosed as a revisable three-month planning date, while Terms 2/3 remain unscheduled. Explicit routes provide calendar/context/forms, locations and a bounded, stable, URL-filtered classes table. Database/Auth, affected browser journeys, unit tests, lint, types, and production build passed. | Fee values remain Phase 3 requirements; no finance records were created. Representative performance testing is deferred to Phase 5 at 200 students, 5,000–10,000 finance records per term, and a 10-user peak. Exact school network speed remains a release-site validation. [Configuration evidence](docs/evidence/phase-1/configuration-verification.md). |
| E1-02 / P1-02 settings hub completion | On 1 September 2026, the obsolete `/settings` Phase 0 placeholder was replaced by a live, permission-protected settings hub. A bounded server summary now shows the configured school identity, current academic year and term, active class/location counts, crest and last update. Three responsive destination cards open the implemented School Settings, Academic Settings and Administrator Access workflows; the remaining Phase 3 finance configuration is disclosed without a fake action. TypeScript passed, React Doctor scored 100, Prettier passed after formatting, and final `pnpm lint`/`pnpm build` exited 0 with `/settings` emitted as a dynamic route. | Authenticated visual verification is pending the separate browser credential-transmission confirmation. The summary is read-only; all mutations remain inside their existing permission-protected workflows. Phase 1 remains complete and finance remains unimplemented. |
| E2-01 / P2-01–P2-02 student slice | On 1 September 2026, MCP applied tracked migration `20260901133516_student_directory_and_onboarding`. Four RLS tables and a security-invoker directory view preserve students, guardians, links, and enrollment history. Direct writes are closed; permission-gated RPCs create one record or a validated batch atomically. `/students` now provides the ordered empty state, bounded URL-filtered 25-row table, Excel export, and reusable preview/validation/duplicate/confirmation import; `/students/new` and `/admissions/new` provide Zod-validated onboarding. Real Auth/RLS, RPC rollback/duplicate checks, targeted unit/import tests, and the affected desktop/tablet/mobile browser journey passed. | Staff, Administrators, student profiles/photos, and enrollment-change UI were not implemented. Student/guardian/enrollment verification fixtures and their synthetic sessions were removed; all four people tables remain empty. No finance schema or records were created. [Student evidence](docs/evidence/phase-2/student-directory-verification.md). |
| E2-02 / P2-01–P2-02 student details extension | On 1 September 2026, MCP applied tracked migration `20260901141827_add_student_disability_and_religion`. Student onboarding now records a required disability Yes/No answer, requires details when Yes, and records religious denomination. The same Zod contract drives manual onboarding and Excel import; previews and exports include the fields. Database constraints/RPC rollback, hosted RLS checks, 11 focused tests, lint, types, build, React Doctor, and the desktop/tablet/mobile onboarding journey passed. | The marked verification transaction rolled back without adding records. One unmarked student/guardian/enrollment bundle and one active session created through the workflow were present at final review and preserved. Existing intentional RPC and Free-plan security-advisor warnings are unchanged. No finance records were created. [Student evidence](docs/evidence/phase-2/student-directory-verification.md). |
| E2-03 / P2-03 student profiles | On 1 September 2026, Supabase MCP applied tracked migrations `20260901143834_student_profiles_enrollment_and_photos` and `20260901144333_require_uploaded_student_photo`. `/students/[id]` now shows personal details, guardian links, chronological enrollment history, private validated photos, and explicitly locked Phase 3 finance sections. Guardian links and enrollment changes are permission-gated transactional RPCs; enrollment changes append history and retain exactly one active row. Private Storage uses RLS, 5 MB/MIME limits, file-signature validation, and authenticated delivery. Hosted Auth/RLS, rolled-back positive writes, 15 focused tests, the real desktop/tablet/mobile browser journey, axe, lint, formatting, types, build, and React Doctor passed. | One unmarked enrollment change made through the application was preserved. Three sessions created by browser verification were revoked by exact ID; the earlier active session remains. Security advisors intentionally flag five authenticated transactional RPCs; each has a fixed search path, active-session permission check, closed direct writes, and negative tests. Free-plan leaked-password protection remains open. [Profile evidence](docs/evidence/phase-2/student-profile-verification.md). |
| E2-04 / P2-04 staff | On 1 September 2026, Supabase MCP applied tracked migration `20260901145746_staff_directory_and_assignments`. RLS-protected staff and assignment-history tables, a security-invoker directory view, audit triggers, and permission-gated transactional create/import/assign/end/archive RPCs keep employment records separate from Auth users. `/staff`, `/staff/new`, and `/staff/[id]` provide the reusable empty/import/export pattern, Zod onboarding, bounded search/pagination, assignment history, guarded archive, audit context, loading/no-results states, and locked salary deductions. One rollback-only database/RLS scenario and one real browser journey passed, followed by lint, formatting, typecheck, build, axe, mobile-fit and React Doctor 100. | Broad unit/coverage suites were intentionally not run per Chief Engineer direction. All exact-scope staff browser fixtures and sessions created during verification were removed; final staff/assignment counts are zero and the one pre-existing session remains. Five new staff RPC advisor warnings are intentional: fixed search paths, active-session permission checks, denied direct writes and focused negative tests cover them. No administrator or finance records were created. [Staff evidence](docs/evidence/phase-2/staff-management-verification.md). |
| E2-05 / P2-05 administrators | On 1 September 2026, Supabase MCP applied three tracked administrator migrations and deployed JWT-verified `administrator-provision` Edge Function version 2. RLS tables, fresh permission checks, AAL2 write gates, transaction locks, audit entries, self-protection, and final-active-Super-Administrator protection secure invitations, role changes, and disablement. `/administrators` reuses the empty/import/export/paginated directory pattern; TOTP enrollment/challenge routes protect privileged writes. One rollback-only DB/RLS scenario and one real browser journey passed, followed by lint, formatting, types, build, axe, mobile fit, and React Doctor 100. | No invitation email or new Auth user was created during verification; positive MFA/email delivery and cleanup remain for P2-06. Four new security-definer RPC notices are intentional and permission/AAL/session checked. Broad suites were omitted per Chief Engineer direction. [Administrator evidence](docs/evidence/phase-2/administrator-management-verification.md). |
| E2-05A / P0-02, P2-05 password-only correction | On 1 September 2026, the Chief Engineer replaced the invitation/MFA design with administrator-issued passwords and mandatory first-login replacement. Supabase MCP applied tracked migrations `20260901162112_force_initial_password_change` and `20260901163026_password_only_administrator_provisioning`; JWT-verified `administrator-provision` version 5 uses Auth Admin `createUser` with confirmed email and never stages passwords in Postgres. Credential-bearing login and password-change traffic moved from Server Actions to size/origin-checked Route Handlers, preventing credential values from appearing in Next Server Action traces. The hosted data reset removed all test Auth/people/admin/session/audit data and created only the named active SUPER_ADMIN with `must_change_password=true`. The real crest and a responsive Framer Motion auth shell passed desktop/mobile browser UI checks with no console errors or overflow; React Doctor scored 100, `pnpm lint` and `pnpm build` exited 0. | Per the Chief Engineer's testing limit, broad suites and a completed password replacement were not run; the supplied temporary password remains unchanged for handoff. Password-only privileged administration is an explicitly accepted alternative to MFA and has higher account-takeover risk. Free-plan leaked-password protection remains disabled. P2-06 remains open. |
| E2-05B / P0-02 auth visual refinement | On 1 September 2026, the login experience was reworked after live visual inspection of Dribbble login results, Pinterest login concepts, and Mobbin's production sign-in. The shared auth shell now uses a restrained editorial split, the real school crest, an image-led classroom context, a compact staff-access form, and reduced-motion-aware Framer Motion entrances. The classroom asset is locally optimized from [Katerina Holmes's Pexels photograph](https://www.pexels.com/photo/black-woman-and-students-during-lesson-5905554/). In-app browser checks at desktop and 390 x 844 confirmed loaded images, working password visibility, no Next.js error overlay, and `scrollWidth === innerWidth`; React Doctor scored 100, TypeScript, `pnpm lint`, and `pnpm build` exited 0. | Pinterest required sign-in for full pin browsing and Mobbin gated its design library, so only visible Pinterest search concepts/original pin assets and Mobbin's public production login were treated as evidence. No authentication behavior or backend state changed in this refinement. |
| E2-05C / P0-05 password length correction | On 1 September 2026, the mandatory first-login replacement rule changed from 15 to 8 minimum characters in the shared Zod contract and visible checklist. Uppercase, lowercase, number, symbol, 128-character maximum, confirmation, and non-reuse checks remain. Supabase MCP documentation confirms eight characters is the recommended lower bound when the strongest mixed-character requirement is used. Boundary verification accepted an eight-character password and rejected seven; typecheck, lint, and React Doctor 100 passed. | This changes application validation only; no database schema, account, password, or remote Auth state was changed. |
| E2-05D / P0-02 school branding | On 1 September 2026, the supplied transparent `logo.png` replaced the bounded JPEG treatment on desktop/mobile sign-in and the protected shell. Supabase MCP applied tracked migration `20260901173121_school_branding_logo_upload`: `school_settings.logo_path`, a public-read `school-branding` bucket restricted to PNG/2 MB, UUID-path insert/delete policies gated by `settings.manage`, and an anon allowlist limited to public branding columns. School Settings now has a permission-protected uploader with client/server MIME, signature, byte-size, and dimension checks; unique immutable object paths prevent stale replacement caches and failed database writes remove the just-uploaded object. The bundled PNG is the resilient fallback while no Storage logo exists. Focused validation passed 3/3, TypeScript and React Doctor 100 passed, Supabase types matched, desktop/mobile in-app browser inspection showed an unclipped transparent crest, anonymous School Settings access redirected to sign-in, and final lint/build exited 0. | No administrator credential or logo upload was used during verification. The branding bucket is intentionally public-read because the crest appears before login; object writes remain RLS-protected. Final database count is zero branding objects, so the supplied bundled PNG is currently displayed until an authorized administrator uploads a replacement. Existing RPC and Free-plan password-advisor warnings are unchanged. |
| E2-06A / P1–P2 route completion | On 2 September 2026, the remaining Phase 1–2 catch-all shells were replaced. `/admissions` now exposes the live bounded admission register and existing validated intake/import/export workflow; `/admissions/[id]` resolves to the authorized student record; class rows open `/classes/[id]`; and `/settings/roles` reads the live RLS-protected role-permission matrix. The local browser verified sign-in, desktop routes, mobile containment and absence of the Phase 0 shell copy. A Next dev-origin mismatch found during testing was fixed with `allowedDevOrigins`, and the login form now has a fail-safe POST action. Lint, typecheck, build and targeted formatting passed; React Doctor found no issues in the changed files. | No database schema, records, role grants or financial data changed. P2-06 remains open for its broader denied-access and mutation journeys. [Route cleanup evidence](docs/evidence/phase-2/phase-route-cleanup.md). |
| E2-06B / P2-06 gate preparation | On 2 September 2026, the runnable local gates for P2-06 preparation passed: `pnpm lint` (0 warnings), `pnpm typecheck`, `pnpm test` (52/52 across 10 files), `pnpm build` (production build with all Phase 1–2 routes emitted), `pnpm test:client-boundary` (34 client chunks, no forbidden server/secret markers), and `pnpm format:check` all exited 0. | `pnpm test:db` was blocked at that moment by removed test credentials; the Chief Engineer subsequently confirmed the required live scenarios were already covered by E2-01–E2-05 and accepted closure (E2-06C). |
| E2-06C / P2-06 closure | On 2 September 2026, the Chief Engineer accepted closure of P2-06, confirming the live Auth/RLS, admission rollback/duplicate, student photo, staff, and administrator verifications were already performed in E2-01–E2-05, with the E2-06A route journeys and E2-06B local gates completing the remaining runnable checks. | Recorded residuals carry forward: Free-plan leaked-password protection, fourteen intentional `SECURITY DEFINER` RPC advisor warnings, partially demonstrated first-password replacement, and representative performance testing deferred to Phase 5. No new database, fixture, or migration work was performed for closure. |
| E3-00 / Phase 3 planning checkpoint | On 2 September 2026, the Chief Engineer authorized Phase 3 one section at a time and limited current work to P3-01. Phase 3 was restructured into six sections; simple daily expenses and official invoice/receipt/voucher templates moved into Finance Core; D-04 received configurable test-project proposals; and Phase 3 verification now centers on actual financial business logic instead of broad E2E automation. [Financial Structure.md](Project%20Files/Financial%20Structure.md) records the supplied cashflow notebook workflow. Focused document formatting/link checks passed; `pnpm lint`, `pnpm typecheck`, `pnpm test` (52/52), and `pnpm build` exited 0. | No SQL, migration, database-type regeneration, application code, test-project data, or Supabase state changed. P3-01 is blocked at the Chief Engineer's requested staff-roster entry checkpoint because the staff-list image has not been supplied. |
| E3-00A / STAFF-01 offline preflight | On 2 September 2026, the staff image and follow-up ID/class rules were captured in [staff requirements](docs/requirements/staff-onboarding.md). A private ignored review contains 18 unique proposed IDs (14 teaching, four non-teaching), confirmed Basic/JHS aliases, two flagged name spellings, and two teachers without supplied classes; unknown contacts, employment dates, and class leadership remain null. Local checks validated those counts/mappings and the review-only import block. `pnpm lint`, `pnpm typecheck`, `pnpm test` (52/52), and `pnpm build` exited 0; these are existing regression checks, not verification of the unimplemented staff extensions. | Supabase MCP tools are unavailable. The public endpoint responded 401 to an unauthenticated reachability check; no school target, migration history, live rows, RLS, or advisors were inspected. No application/SQL changes or imports occurred. Current staff constraints still require phone, uppercase IDs, and class-only assignments. Reconnect MCP, implement/verify the compatibility change, and resolve flagged roster details before import; P3-01 remains unstarted. |

E3-00B / STAFF-01 corrections and MCP reconnect (2 September 2026): the Chief Engineer's corrected spellings for proposed IDs 004/006 were applied to the private Markdown/JSON preview and their uncertainty flags cleared. Local validation confirmed both corrections, 18 roster records, zero unresolved name flags, private Git exclusion, and the unchanged review-only import block; requirements formatting passed. The bundled Codex CLI found the existing Supabase server enabled, OAuth configured, and its URL scoped to test project `cefwopisbgfctzdloequ`. The default login failed at dynamic client registration with HTTP 400 for unsupported scopes. A retry using explicit supported scopes (`organizations:read`, `projects:read`, `database:read`, `database:write`, `analytics:read`) completed browser consent and returned `Successfully logged in to MCP server 'supabase'`. Post-login configuration remained enabled and test-project-scoped. The current task still exposes no Supabase tools/resources; restart the MCP connection in Codex Settings and recheck tool discovery before any database work. No application code, schema, staff rows, or financial records changed. No current remote verification or new application-test run is claimed, and the earlier tool absence is not proof of expired credentials.

E3-00C / STAFF-01 implementation and known-details onboarding (2 September 2026): Supabase MCP restored and test target verified. Migration 17 adds optional details, preserved full names, server IDs, class-subject pairs/separate headship, replay protection and atomic audit. Types regenerated; focused rollback/RLS/retry SQL passed. All 18 staff saved through authenticated Add Staff forms and privately reconciled; 18 requests/audit entries and no fabricated assignments. The private generated workbook was incompatible with ExcelJS; format errors now explain that no data was saved. Local lint/typecheck/tests (68/68)/build and responsive checks passed. Assignment decisions and overlapping concurrency remain pending; no finance work. [Verification evidence](docs/evidence/phase-2/staff-known-details-verification.md).

E3-01 / Phase 3 planning breakdown (3 September 2026): re-read Plan.md, AGENTS.md, Project.md, Financial Structure.md, the original project guide's finance sections, and the STAFF-01/D-04 requirements before any Phase 3 coding. Added the Phase 3 execution breakdown (§7) decomposing P3-01–P3-06 into smaller internally-sequenced steps without changing the stable task IDs/dependencies. The Chief Engineer was unavailable to answer clarifying questions interactively; recorded the agent's proposed defaults as D-10, pending confirmation, rather than assuming approval. No SQL, migration, type regeneration, or application code changed.

E3-02 / P3-01 step (a): finance reference-configuration schema (3 September 2026): the Chief Engineer confirmed D-10 (see above) and directed starting P3-01 in parallel with the open STAFF-01 roster facts. Supabase MCP applied tracked migration `20260903130000_finance_reference_configuration`, matching local SQL. Added `fee_components` (generic, effective-dated engine; scope in `class`/`location`/`flat`), `fee_component_rates` (effective-dated by academic year + term, with a trigger enforcing year/term consistency and scope-matched class/location references, plus a unique index preventing duplicate active rates), `payment_methods`, `expense_categories`, and `misc_income_categories`. All five tables have RLS: read gated by `financials.read`, write gated by a new `finance.settings.manage` permission granted only to `SUPER_ADMIN` (mirroring the existing `settings.manage` sensitivity tier; `ADMINISTRATOR` does not yet have it). Seeded the two active fee components and the confirmed 2026/2027 Term 1 rates from Financial Structure.md §5.2/§5.3 (four class-group base fees across 13 classes, five location/transport charges), plus the proposed payment methods and 11 expense categories from §16. No misc-income categories were seeded — that list remains an open Chief Engineer decision (§17), so the table is intentionally empty. Verified: anon read denied, mismatched component-scope insert rejected by the trigger, and a duplicate active rate rejected by the unique index (all rolled back). No new security-advisor findings. Types regenerated; `pnpm lint`, `pnpm typecheck`, and `pnpm test` (68/68) passed. No settings UI, invoice/payment/expense tables, or RPCs were built yet — those remain P3-01 steps (b)–(h) and P3-02–P3-06.

E3-03 / P3-01 step (b): invoices, invoice lines, and document numbering (3 September 2026): Supabase MCP applied tracked migration `20260903140000_finance_invoices_and_numbering`, matching local SQL. Added `private.document_number_counters` (deny-all, mirrors the existing `staff_number_counter` pattern) and `private.allocate_document_number(document_type)`, a row-locked, per-document-type-per-year sequence generator using the confirmed `BBA/{TYPE}/{YYYY}/{NNNNN}` format for `INV`/`PAY`/`RCT`/`EXP`/`REV`; committed calls returned `BBA/INV/2026/00001` then `BBA/INV/2026/00002`, confirming correct increment. Added `public.invoices` (student/year/term/class/location references plus immutable identity snapshots — student name, admission number, class name, location name — so a later rename does not alter an issued invoice; generated `outstanding` column; a partial unique index allows only one non-cancelled invoice per student per term) and `public.invoice_lines` (immutable component/description/amount rows). Both tables follow the closed-direct-write pattern already used for `staff`/`students`: RLS enabled, only `SELECT` granted to `authenticated` (gated by `financials.read`), no insert/update grants at all — every write must go through a future `SECURITY DEFINER` RPC (P3-03). No RPCs exist yet, so these tables cannot be written to at all right now; that is intentional until P3-03. Security advisors show only the expected new INFO (deny-all counter table) and the same pre-existing residuals. Types regenerated; `pnpm lint`, `pnpm typecheck`, and `pnpm test` (68/68) passed.

E3-04 / P3-01 step (c): payments, receipts, and shared request idempotency (3 September 2026): Supabase MCP applied tracked migration `20260903150000_finance_payments_receipts_and_idempotency`, matching local SQL. Added `private.finance_requests` (deny-all idempotency table shared by every future P3-04 RPC — school-fee payment, feeding/admission/misc receipts, expenses), `public.payments` (invoice reference, amount, method, business date; reversal columns included now rather than deferred to a later ALTER), and `public.receipts` (1:1 with a payment via a unique FK; immutable identity/invoice/method/collector snapshots plus previous/remaining balance). A new trigger, `private.validate_payment_reference`, rejects a payment missing `external_reference` when its `payment_methods.requires_reference` flag is true (verified: a Mobile Money test insert was correctly rejected; a Cash insert with `status = 'reversed'` but no `reversed_at` was correctly rejected by the existing reversal-fields check). Both tables use the same closed-direct-write pattern as invoices (RLS, `SELECT`-only grant gated by `financials.read`, no RPCs yet — P3-04). Anon read denial reconfirmed. No new security-advisor findings. Types regenerated; `pnpm lint`, `pnpm typecheck`, and `pnpm test` (68/68) passed.

E3-05 / P3-01 steps (d)+(e) and schema-wide (h) verification pass (3 September 2026): the Chief Engineer asked for every remaining schema-only step to be applied together. Supabase MCP applied tracked migration `20260903160000_finance_receipts_and_expenses`, matching local SQL, adding `public.feeding_receipts` (unique-per-student-per-active-day), `public.admission_receipts` (unique-per-student-while-active, so a student can only ever have one active admission-fee receipt), `public.misc_receipts` (category + optional student/payer), and `public.expenses` (category + optional attachment path). All four reuse the existing `private.validate_payment_reference` trigger (its columns — `payment_method_id`/`external_reference` — match across every finance table), each has its own reversal columns/check (folding step (f) into table creation instead of a later ALTER), and all follow the closed-direct-write pattern (RLS, `SELECT`-only grant gated by `financials.read`, no insert/update grants). Verified: anon read denied on `feeding_receipts`; a Mobile Money `expenses` insert without a reference was rejected by the reused trigger; a Cash `expenses` insert succeeded (rolled back). A full schema-wide pass (`list_tables`, security + performance advisors) confirmed RLS is enabled on all 14 finance-related tables, zero rows outside the seeded config tables, and no new WARN/ERROR advisor findings beyond the existing documented residuals. Types regenerated; `pnpm lint`, `pnpm typecheck`, `pnpm test` (68/68), and `pnpm build` all passed. P3-01's schema is now complete except for the actual RPCs, which are P3-03 (invoices)/P3-04 (payments/receipts/expenses)/P3-05 (reversals) — separate authorized tasks, not opened in this pass.

E3-06 / P3-02 financial settings UI (3 September 2026): before implementing, re-read [design.md](Project%20Files/design.md) and the `gpt-taste` skill; design.md explicitly overrides gpt-taste's marketing/AIDA/GSAP defaults for this administrative app, so the new screen reuses the existing settings-hub pattern (panels, `configuration-disclosure` accordions, `FormField`/`Input`/native `select`, `StatusBadge`) rather than any hero/bento/motion treatment. First applied migration `20260903170000_finance_feeding_and_admission_fee_components`, adding `feeding_fee`/`admission_fee` as `scope='flat'` rows in the existing `fee_components` engine (not invoice line items — feeding/admission remain separate receipt flows) with the confirmed GHS 10.00/GHS 50.00 2026/2027 Term 1 rates; verified the scope-validation trigger correctly required both `class_id`/`school_location_id` to stay null. Built `features/finance/` (schemas with a decimal-string `moneyAmountSchema`, queries, actions, components) and the new `/settings/financials` page: editable base-class-fee/transport-charge tables for the current term, a feeding/admission amount form, payment-method/expense-category/misc-income-category CRUD (reusing the existing add/edit disclosure pattern), and a read-only document-numbering reference panel. Added permission `finance.settings.manage` (SUPER_ADMIN-only, mirrors `settings.manage`) gating every write; reads gated by `financials.read`. Fixed a real defect caught by typecheck: Supabase's generated types map `NUMERIC(14,2)` to `number` on both read and write, so amounts are converted to/from the validated decimal string only at that unavoidable client boundary (`toRateAmount`/`formatRateAmount`), never during validation or business logic. `pnpm lint`, `pnpm typecheck`, `pnpm test` (68/68), and `pnpm build` all passed, with `/settings/financials` emitted as a route. Live authenticated browser screenshots (desktop/tablet/mobile) and React Doctor were not run this pass — no authenticated browser session was available and no `react-doctor` tool/script is currently installed in this workspace; this is a reported limitation, not a passed check.

E3-07 / P3-03 invoice generation and cancellation RPCs (3 September 2026): applied migration `20260903180000_finance_invoice_generation_rpcs` (new `finance.transactions.manage` permission for SUPER_ADMIN/ADMINISTRATOR/ACCOUNTANT; `private.generate_invoice_for_student` computing subtotal/total from the active base-class-fee and location/transport-charge rates for the student's enrollment, snapshotting identity/class/location and using the configurable `location_charge_label`; `public.generate_term_invoices` for both bulk per-term and single-student generation per D-10, isolating each student's failure in its own sub-transaction so one bad row cannot abort the batch; `public.cancel_invoice` rejecting invoices that are already cancelled or have payments recorded) followed by a same-day fix migration `20260903181000_finance_generate_term_invoices_return_details` after testing surfaced that the first version only returned a count, not the created invoice IDs/numbers callers need. Verified end to end with rolled-back transactions impersonating the real signed-in Super Administrator's own active session (not a different identity): single-student generation for a JHS 1 student at Anomaa Kojo produced GHS 1,832.00 (207.00 base + 1,625.00 transport), matching Financial Structure.md's verified term-fee matrix exactly; a second attempt for the same student/term was correctly skipped; bulk mode correctly skipped the same already-invoiced student without erroring; cancellation succeeded once, correctly freed the student for a fresh invoice, and correctly rejected a second cancellation attempt; an anonymous call was denied. Types regenerated; security advisors show no new findings; `pnpm lint`, `pnpm typecheck`, `pnpm test` (68/68), and `pnpm build` all passed. Not done: invoice list/detail views (unlocking the student finance tab) and the A4 invoice / compact receipt-voucher document templates — remaining P3-03 sub-steps.

E3-08 / P3-03 invoice list/detail views and A4 document (3 September 2026): added `getInvoicesPage`/`getInvoiceDetail` to `features/finance/server/queries.ts` (avoiding PostgREST embed syntax, matching the codebase's existing separate-query-plus-JS-join convention) and `generateTermInvoices`/`cancelInvoiceAction` server actions calling the P3-03 RPCs. Added new permission-gated routes: `/financials/invoices` (bounded 25-row table with status/search filters, a `GenerateInvoicesPanel` for authorized `finance.transactions.manage` users, empty state) and `/financials/invoices/[id]` (full invoice detail styled as a printable A4-proportioned document — school identity header, student/class/transport-location context, itemized immutable lines, subtotal/total/amount-paid/outstanding, a `VOID` banner with reversal metadata for cancelled invoices, and a browser print button using `print:hidden` to hide chrome; `CancelInvoiceForm` shown only when unpaid and not already cancelled). Added `Cancelled`/`Reversed` entries to the shared `StatusBadge` styles. Fixed two typecheck defects: the new `finance.transactions.manage` permission was missing from the shared `Permission` union (added, with its roles page label), and the RPC's `target_student_id` parameter expected `number | undefined`, not `number | null` (fixed with `?? undefined`). This is a browser-printable HTML document (no PDF library), consistent with Project.md's guidance to add PDF dependencies only when there's a concrete need — a true PDF export can follow once payments/receipts exist and a document set is reviewed together. `pnpm lint`, `pnpm typecheck`, `pnpm test` (68/68), and `pnpm build` all passed, with both routes emitted; confirmed 0 real invoice rows in the test project (all prior verification data was rolled back). Live authenticated browser/print screenshots were not inspected this pass — no session available.

E0-06 implementation checkpoint: commit `2e1ab95` (`Complete Phase 0 authentication gate`). It is local and has not been pushed or deployed.

Historical local regression after E0-04/E0-05 (31 August 2026): `pnpm lint`, `pnpm format:check`, `pnpm typecheck`, `pnpm test` (25 tests), `pnpm test:coverage` (96.66% lines / 93.10% branches), `pnpm build` and `pnpm test:client-boundary` all exited 0. Production-mode synthetic `pnpm test:e2e` passed 21/21 in 55.0 seconds across desktop/tablet/mobile. The then-current real-provider blockers and empty advisor result are superseded by E0-06.

For future entries, retain these fields:

| Field | Required content |
| --- | --- |
| Task / date / owner | Stable task ID, verification date, responsible agent. |
| Change | Files and commit if available; schema/migration identity where applicable. |
| Checks | Exact commands, exit codes, test cases, and results; explain skips. |
| Browser / database / performance | Relevant tested roles, viewports, scenarios, query plans, measurements, and artifact locations. |
| Remaining risk | Known limitations, dependency, recovery implications, or none observed within the tested scope. |

Link larger logs/reports when created; keep this ledger readable and redact sensitive information. Reopen affected tasks when later changes invalidate their evidence.

### Current handover

**E5-01 / P5-01–P5-03 and P5-04 progress, 9 September 2026:** Applied four reporting migrations through Supabase MCP, aligned local/remote migration history and database types, and added permission-gated daily/weekly/monthly/term aggregates plus the normalized financial activity view. Replaced dashboard demo data with live records, added `/financials` and `/reports`, and delivered 13 bounded reports, student statements, CSV/Excel export protection, and browser Print / Save PDF styling. Rollback-only SQL reconciled exact totals, term scope, reversals, fresh role revocation and anonymous denial; a focused test verified formula safety and a complete 10,000-row workbook. Authenticated browser inspection covered reconciled dashboard/report values, a live Excel download, and tablet/mobile layouts. A 200-student/10,000-row benchmark measured the snapshot at 54.59 ms and the report page at 20.54 ms with zero residue; ten authenticated production-build report tabs completed without functional errors, though the serialized harness timings are not latency evidence. Format, lint, typecheck, build, and React Doctor review passed without a new actionable Phase 5 defect. No broad E2E suite was added or run. Full evidence: [Phase 5 reporting verification](docs/evidence/phase-5/reporting-verification.md).

**E5-02 / P5-04–P5-05 closure, 9 September 2026:** Replaced dashboard recent collections with a bounded outstanding-fees table and class filter, moved recent collections to Financial Overview, and added weekly, monthly, term, and academic-cycle Revenue/Expense/Net summaries with explicit totals and matching exports. The focused grouping/export tests passed 6/6; authenticated desktop/tablet/mobile browser checks covered all four periods, the dashboard class filter, and the relocated collections with no console errors or document overflow. A rollback-only authenticated 10,001-receipt payment-list benchmark measured 14.90 ms combined and retained zero fixtures. A fixed 400 ms response-delay profile produced a usable dashboard in 2.39 seconds and weekly report in 2.06 seconds; the temporary proxy was stopped and only port 3000 remained listening. A three-page A4 report passed full-page and grayscale inspection without clipping or overlap, and its temporary artifacts were removed. Format, lint, typecheck, build, and React Doctor review passed; no broad E2E suite was added or run. Phase 5 is verified complete. Full evidence: [Phase 5 reporting verification](docs/evidence/phase-5/reporting-verification.md).

**E6-01 / P6-01 security hardening, 9 September 2026:** Audited all 37 page routes and 17 API route files, then verified 35 protected pages and 15 protected API methods live on port 3000. Closed origin and unbounded-body gaps, added nonce CSP/security headers, introduced database-owned per-account limits across sensitive writes/search/import/export/upload/password flows, and deployed JWT-verified `administrator-provision` Edge Function version 6 with bounded input and rate limiting. MCP applied two tracked migrations; a rollback-only 10-check database proof passed. All 35 public tables have RLS, all three views are security-invoker, anonymous table/view access is zero, and all 35 authenticated definer functions have fixed empty search paths plus active-session authorization boundaries. Format, lint, typecheck, 14 focused tests, route/live security checks, client-boundary scan, production build, and production dependency audit passed; no broad E2E suite was added or run. Full evidence: [Phase 6 security hardening](docs/evidence/phase-6/security-hardening.md).

**E6-02 / P6-L01 Library Books & Prospectus, 10 September 2026:** Applied tracked migrations `20260910120000_library_books_prospectus` and `20260910130000_consolidate_payment_method_read_policy` to the test-only project, adding effective-term/class rates, immutable charge snapshots, a separate retry-safe and reversible Library collection ledger, RLS/permission boundaries, and the Librarian / Book Keeper role. Added the `/library` workspace, separate student invoice bill, and separate financial Expected/Paid/Net KPIs without changing school-fee totals or the accounting activity report. Nursery 1/2 remain visibly unconfigured; JHS 3 is explicitly not charged. Focused rollback-only SQL and permission tests passed; authenticated desktop/mobile browser checks covered Library, invoice, and Financial Overview with no console errors. Format, lint, typecheck, explicit route-security, client-boundary, and production-build gates passed without a broad E2E suite. Full evidence: [Library Books & Prospectus verification](docs/evidence/phase-6/library-books-prospectus.md).

**E6-03 / Financial settings module navigation, 10 September 2026:** Reorganized the long Financial Settings page into four focused navigation cards matching the established Settings landing-page pattern: Student fees, Salaries, Payment setup, and Financial categories. Selecting a card renders only that module's existing forms below the card grid; permissions, financial rules, save actions, and data contracts are unchanged. Authenticated browser inspection opened all four modules at a 638 × 958 viewport, confirmed semantic headings/links and no horizontal document overflow, and found no console warning or error. The required GPT Taste review was applied with the dashboard-specific project overrides. React Doctor's one new giant-component finding was resolved by extracting focused server components, returning changed-scope findings from 33 to the pre-existing 32. `pnpm format`, `pnpm lint`, `pnpm typecheck`, and `pnpm build` exited 0; no E2E suite was added or run.

**E6-04 / Salary-reporting semantics correction, 10 September 2026:** The Chief Engineer clarified that salary posting is calculation-only, not proof of employee payment. Applied tracked migration `salary_deductions_reporting_semantics`, preserving posted deductions and the salary-register gross-minus-deductions calculation while excluding deductions from operating net and recorded cash position. The report labels deductions as informational and explains when cash will change. A focused rollback-only database proof used a GHS 500.00 salary with GHS 27.50 SSNIT: the deduction remained in the detailed and weekly reports, while operating net and cash position were unchanged. Standard quality and focused UI evidence are recorded in [Salary-reporting semantics](docs/evidence/phase-6/salary-reporting-semantics.md); no E2E suite was added or run.

**E6-05 / P6-S01 salary cash workflow, 10 September 2026:** Implemented distinct partial/full employee salary-payment and SSNIT employee-contribution remittance workflows against posted salary calculations. Applied tracked migrations `salary_cash_workflow` and `salary_cash_scalar_deduction_fix`; active cash creates exactly one linked outgoing expense, drives exact paid/outstanding statuses, rejects overpayment and unsafe dependency changes, is retry-safe, and reverses without deleting history. The RPC denies anonymous execution, uses a fixed empty search path, locks the salary row, and checks the active user's finance-management permission; the read view is security-invoker over RLS tables. One rollback-only SQL proof covered partial/completed payments, remittances, retries, reversals, and report reconciliation with zero residue. Authenticated localhost inspection verified September calculations and both workflows without recording cash, while August remained unposted. Format, lint, typecheck, build, and changed-scope React Doctor review passed; no broad unit or E2E suite was added or run. Full evidence: [Salary cash workflow](docs/evidence/phase-6/salary-cash-workflow.md).

**E6-06 / P6-S01 salary bulk workflow, 10 September 2026:** Added the two-step one-click monthly workflow requested by the Chief Engineer. Post all creates only missing configured salary calculations and does not reduce cash; dispatch all is available only after all eligible salaries are posted, pays each exact outstanding net salary in one atomic transaction, skips completed records safely, and leaves SSNIT separate. Supabase MCP applied tracked migrations `salary_bulk_workflow` and `salary_bulk_dispatch_guard`; both RPCs use active-session finance authorization, fixed search paths, bounded timeouts, deterministic month/row locks, and retry-safe request records, with anonymous execution denied. The forward guard enforces post-before-dispatch in the database as well as the UI. One rollback-only focused SQL proof passed incomplete-roster rejection, exact, replay, validation, rollback, payment, SSNIT-separation, and permission checks with zero synthetic residue. Desktop/mobile inspection passed with empty browser error logs; format, lint, typecheck, build, and React Doctor review passed without a new finding in the bulk component. No broad unit or E2E suite was added or run. A verification-side batch request created the 15 missing September calculations but no cash transaction; August remains unposted. The Chief Engineer must decide whether those September calculations should remain or be audit-reversed. Full evidence: [Salary bulk workflow](docs/evidence/phase-6/salary-bulk-workflow.md).

**E6-07 / P6-R01 role workspace access foundation, 15 September 2026:** Applied tracked migration `20260915145048_role_workspace_access_foundation` through Supabase MCP, reset the four operational roles to the D-14 least-privilege matrix, added narrow Outstanding Fees read/print permissions and invoice RLS, and presented the compatibility `MANAGEMENT` code as Board Member. Accountants now receive read-only Student/Staff access plus the complete finance/report/settings workspace; Administrators retain Student operations and Staff read-only access but receive only the Outstanding Fees finance boundary and cannot process payments. Navigation, route guards, role selectors, imports/exports, and outstanding-table detail links were aligned. Exact database grants/policy, 8 focused permission tests, authenticated desktop/mobile browser checks with an empty console, lint, formatting, typecheck, and production build passed. React Doctor reported no finding in changed files; no broad E2E suite ran. Filtered printing and role-specific dashboards remain, so P6-R01 stays in progress. Full evidence: [Role workspace access foundation](docs/evidence/phase-6/role-workspace-access-foundation.md).

**E6-08 / P6-R01 Administrator outstanding-fees workflow, 15 September 2026:** Added a bounded 25-row outstanding-balance table with student/invoice search, class and academic-term filters, URL-preserved pagination, and a dedicated print route that reruns the same normalized filter set. The report shows school identity, the active scope, exact total outstanding, repeated headers, and A4 landscape print styling; a 1,000-row safety cap is explicit. Main and print routes independently enforce the narrow E6-07 permissions, and student/invoice links remain permission-gated without exposing a payment control. One focused query-contract file passed 2/2; authenticated desktop/mobile browser checks verified the combined filters, report reconciliation, horizontal table handling, and empty console. Lint, formatting, typecheck, build, and React Doctor review passed without a new finding in this slice; no broad E2E suite or account/data mutation ran. P6-R01 remains in progress for role-specific dashboards and per-role negative-access journeys. Full evidence: [Administrator outstanding-fees workflow](docs/evidence/phase-6/administrator-outstanding-fees.md).

**E6-09 / P6-R01 role-specific dashboards, 15 September 2026:** Added server-selected dashboard compositions for Administrator, Accountant, Board Member, Librarian, and Super Administrator. Administrator receives enrollment, admissions, teaching-staff, and view/print-only outstanding-fee follow-up; Accountant retains the finance workbench; Board Member receives aggregate read-only oversight without the student-level balance table; Librarian receives term collection and charge coverage; Super Administrator receives finance plus cross-school operations and access totals. Each branch limits its server queries to its selected composition and relies on the E6-07 grants/RLS/server authorization. A focused resolver file passed 6/6, authenticated Super Administrator desktop/mobile inspection showed no page-level overflow or fresh console issue, and lint, format/check, typecheck, build, and changed-scope React Doctor review passed. No account/role/data mutation or broad E2E suite ran. P6-R01 remains in progress for per-role authenticated negative-access journeys. Full evidence: [Role-specific dashboards](docs/evidence/phase-6/role-specific-dashboards.md).

**E6-10 / P6-R01 role Settings and Board oversight closure, 15 September 2026:** Added a role-aware Settings landing for Administrator, Accountant, Board Member, Librarian and Super Administrator without widening child configuration permissions. Revised Board Member to cross-school read-only operational access and applied tracked migration `20260915162417_board_member_cross_school_read_access`, whose exact ten-permission result excludes every mutation and Super Administrator permission. Replaced the Board's generic summary with six requested financial KPIs, Month/Academic-term/Academic-cycle filtering, a revenue/expense/net trend chart and a fee-position chart. Four temporary role-test accounts completed authenticated positive and negative journeys; the Board could open all operational read pages while write controls, Financial Settings and Administrators stayed unavailable. The temporary users and provisioning requests were then removed with zero orphans, and the one active Super Administrator chain was preserved. Focused 15/15 tests, typecheck, formatting, lint, production build and fresh browser logs passed; React Doctor named no file from this slice. P6-R01 is verified complete. Full evidence: [Board oversight and role Settings](docs/evidence/phase-6/board-oversight-and-role-settings.md).

**E6-11 / P6-01 PWA, navigation and live search, 15 September 2026:** Added an installable, online-only PWA shell with deployment-aware worker updates and an explicit in-app update notice; the worker has no fetch handler or protected-data cache. Added permission-aware breadcrumbs, activated the top-bar search for permitted pages and bounded RLS-protected school records, and made list/report filters update their URL-backed results live without Apply actions. Authenticated localhost checks covered page and record search, Administrator and Invoice filtering, nested breadcrumbs, desktop/mobile layout and an empty browser console. Format, lint, typecheck, route-security, client-boundary, 19 focused tests, production build and manifest/worker inspection passed. React Doctor's shared-filter findings dropped by eight; remaining findings are pre-existing outside the new shell components. P6-01 remains in progress for D-05 production environment, recovery and monitoring decisions. Full evidence: [PWA, navigation and live search](docs/evidence/phase-6/pwa-navigation-and-search.md).

**E6-12 / P6-F01 finance oversight and end-of-term invoices, 15 September 2026:** Replaced the Financial Overview and Accountant landing composition with finance-operations oversight: cash movement, gross receipts, expenses, operating net, recorded cash position, fee recovery, Library exposure, income/expense breakdowns and recent collections, using the Board period engine with a distinct finance workflow. Applied tracked migrations `20260915172552_end_of_term_invoices` and `20260915174034_consolidate_invoice_read_policies`. The new workflow snapshots next-term school-fee lines, source-term outstanding balance, Books & Prospectus amount and a locked parent note; it generates retry-safe 50-student batches by school/class and prints 25-document batches. Accountant, Administrator and Super Administrator can configure/generate; Board is RLS-verified read/print-only. A rollback-only synthetic proof passed twice after policy consolidation and left zero test residue. Term 2 correctly shows one school-fee gap and one prospectus gap, so generation remains disabled without invented rates. React Doctor reports no finding in the new dashboard, document, manager or PWA update component; 24 pre-existing changed-scope warnings remain. Format/check, lint, typecheck, 24 focused tests, route-security, client-boundary, production build, authenticated desktop/mobile/print browser checks and empty logs passed. Full evidence: [Finance oversight and end-of-term invoices](docs/evidence/phase-6/finance-oversight-and-end-term-invoices.md).

**E6-13 / Pre-push application review, 15 September 2026:** Completed a repository, dependency, route, client-boundary, live Supabase, authenticated browser and responsive UI review without expanding release scope or changing financial data. The focused corrective pass removed a Student filter search-parameter rendering dependency, server/client month-default drift, unstable skipped-invoice keys, repeated formatter construction, an unnecessary duplicate Library Supabase client and an ambiguous conditional-render expression. The healthy test project has RLS on all 40 public tables, no `anon` table grants, four `security_invoker` views, no anonymously executable public definer RPCs, and caller guards on every authenticated definer RPC; the trigger-only RLS helper is not client-executable. Dependency audit, 113 tests, formatting, lint, typecheck, static and live route-security, client-boundary, production build, authenticated desktop/mobile journeys, live/global search, end-term disabled-state, horizontal-overflow checks and empty browser logs passed. The live probe covered 40 protected pages, 16 API methods, auth mutation defenses and security headers. Remaining Supabase advisor notices are the documented intentional permission-checked RPC architecture, private deny-by-default tables, and Free-plan leaked-password setting; D-05 recovery/monitoring and P6-05 release acceptance remain open. Full evidence: [Pre-push application review](docs/evidence/phase-6/pre-push-application-review.md).

**E4-02 / P4-01–P4-04 salary configuration extension, 9 September 2026:** The Chief Engineer resolved the August roster gap: the schedule-only person was added as an active non-teaching cook, and the roster-only person remains active without a salary configuration because they are paid outside the register. Applied tracked migration `20260909152652_staff_salary_configurations`, adding effective-dated salary configuration history, permission-gated retry-safe add/change/end RPCs, and configured-rate lookup when a monthly salary is posted. Financial Settings now supports adding, changing, and ending salary rates without deleting history; the salary register no longer accepts a manually typed gross amount. The 18 configured August rates reconcile to GHS 12,450.00, while `salary_records` remains empty. The focused rollback-only salary SQL file passed, authenticated browser inspection verified both decisions and the zero-state August register with no console errors or horizontal overflow, and format/lint/typecheck/build passed. React Doctor reduced changed-scope findings from 34 to 30 by resolving all four findings introduced or exposed in the changed salary files; remaining findings are pre-existing outside this slice. No broad E2E or general unit suite was added or run. Full evidence: [Salary configuration reconciliation](docs/evidence/phase-4/salary-configuration-verification.md).

- **Active scope (10 September 2026):** Phase 6 is active. P6-L01 and P6-S01 are verified complete. P6-01 remains in progress after completion of its security-hardening slice because D-05 hosting/domain/region, retention, recovery objectives, backup coverage/cost, and monitoring ownership are unresolved.
- **Role workspace progress (15 September 2026):** P6-R01 is verified complete through E6-10. D-14 now combines the Accountant and Administrator least-privilege boundaries with cross-school read-only Board oversight, the D-12 Librarian workspace, a complete Super Administrator workspace, and role-aware Settings for all five roles. Authenticated positive/negative journeys passed and all four temporary role-test accounts plus provisioning requests were removed without disturbing the active Super Administrator.
- **Application shell progress (15 September 2026):** E6-11 verifies an online-only installable PWA, user-controlled update notice, permission-aware breadcrumbs and global search, and live URL-backed filters. No Web Push permission, offline protected-data cache, role grant, schema change, or school-record mutation was introduced. Production deployment/recovery/monitoring decisions remain under D-05.
- **Finance workflow progress (15 September 2026):** E6-12 verifies a finance-operations dashboard and a separately permissioned end-of-term next-term invoice workflow. Generation is intentionally unavailable until Term 2 school-fee and Books & Prospectus rates are configured; no real invoice/configuration or test account remains in the database.
- **Pre-push review (15 September 2026):** E6-13 verifies the current application candidate after focused bug, security and UI corrections. No school or financial record was mutated. This does not resolve D-05, authorize production promotion, or replace the future P6-05 release suite.
- **Salary data decision:** The source-to-roster mismatch is resolved and all 18 scheduled rates are configured. Keep the entire August salary batch unposted until the Chief Engineer separately authorizes posting. During E6-06 verification, 15 missing September calculations were created by a completed batch request, bringing September to 18 active calculations; no salary-payment expense or SSNIT remittance was created. Await the Chief Engineer's explicit retain-or-audit-reverse decision before correcting those records.
- **Salary reporting decision:** Salary posting creates a calculation, not a payment. GHS 27.50 is specifically the automatic 5.5% SSNIT employee contribution on GHS 500.00. Posted deductions remain informational; active employee payments against net salary and active SSNIT remittances against the contribution are separate partial/full cash workflows that reduce operating net through the existing expense ledger. Post all creates missing calculations only; dispatch all pays exact outstanding net salary atomically and keeps SSNIT separate (D-13, E6-04, E6-05, E6-06).
- **Next work:** Resolve D-05/P6-01 release-environment, recovery, and monitoring work or begin the separately authorized P6-02 import-template slice. Separately obtain the retain-or-audit-reverse decision for the 15 September calculations. Do not import additional client data until P6-02 validation is complete; preserve the explicitly unposted August salary batch and the Phase 6 on-site network validation.

**E4-01 / P4-01–P4-04 Phase 4 closure, 9 September 2026:** The Chief Engineer opened Phase 4 with the supplied August salary structure and requested focused verification only. Applied `20260909095828_salary_records_and_deductions`, `20260909100512_qualify_salary_record_result`, and `20260909101703_salary_precision_and_reversal_reason`; added effective-dated percentage/fixed deduction types, automatic 5.5% SSNIT, monthly gross/deduction/net snapshots, unique references, retry-safe posting, audited reversals and corrected replacement. Implemented the salary register/detail screens, settings editor, and staff salary history. A rollback-only transactional SQL check passed and left no residue; authenticated localhost inspection covered the new screens. Standard format/lint/type/build gates and React Doctor were used; no broad E2E or general test suite was added. The real August batch is deliberately unposted because one supplied identity does not match the current roster and one roster identity is absent from the schedule. Full evidence: [Phase 4 salary register verification](docs/evidence/phase-4/salary-register-verification.md).

- **Prior active scope (9 September 2026):** Phase 4 was verified complete and Phase 5 awaited authorization. Superseded by E5-01 above.
- **Data decision required:** Confirm the single source-to-roster identity mismatch before any real August salary records are posted. Do not partially post the batch or infer the missing mapping.
- **Prior next work:** Phase 5 authorization was subsequently supplied and implementation began in E5-01.

**E3-12 / GitHub Actions E2E scope, 9 September 2026:** At the Chief Engineer's request, the always-on `local-quality` job no longer installs Chromium or runs the broad synthetic `pnpm test:e2e` suite, and its now-unused screenshot artifact upload was removed. The local Playwright command remains available for focused or release checks. The manually dispatched, isolated Supabase `pnpm test:e2e:auth` job remains unchanged. This follows D-08 and does not claim the attached failing Payments journey is fixed.

**E3-11 / Payments workspace, 8 September 2026:** The Chief Engineer confirmed Payments is also part of Phase 4. Its Phase 3 payment history now has a concrete `/financials/payments` route instead of the Phase 0 shell, reused by Phase 4 operations. It shows immutable student/invoice/method snapshots, payment references, amounts, status/reversal reasons, and working invoice/receipt links. Added student/invoice/receipt search, business-date/status filters, stable 25-row pagination, and permission-gated links to existing posting/reversal workflows. No new ledger or schema was introduced. Verified the existing payment, invoice-search match, no-results state, clear filters, receipt navigation and mobile layout. Formatting, lint, two focused query tests and production build passed. No transactions were posted or reversed during verification.

**E3-10 / Phase 3 documents and fee structure, 8 September 2026:** Added the school crest and a shared document header to invoices, receipts and expense vouchers. Global print CSS removes app navigation, search/account header, page controls and sidebar offsets. Invoice footer now gives parents payment-reference guidance instead of recorded-by details. Fixed receipt rendering by formatting database monetary values as two-decimal strings. Implemented `/financials/fees` with term selection, configured class/location totals, separate feeding/admission rates, missing-rate states and permission-gated reuse of the fee editing forms. Browser checks covered the reported receipt, term switching, validation and responsive layout; a focused Chromium print-media check confirmed hidden chrome and a loaded logo. Three fee arithmetic tests, formatting, lint and production build passed. Verification was deliberately limited to the Chief Engineer's requested checks; no finance data or rates were changed, and broader Phase 3 gates remain separate.

**E3-09 / Cashflow corrections, 8 September 2026:** Implemented the Chief Engineer's seven cashflow changes and safe diagnostic logging. Verified the original SQL error and applied the two tracked fixes through Supabase MCP. Rolled-back SQL regression passed; the application suite passed 85 tests and the final focused finance suite passed 18 tests. Lint, format, type checks and production build passed. Live invoice search, conditional fields, rejected posting and desktop/tablet/mobile layouts were inspected on the existing port-3000 server. React Doctor findings and remaining phase-wide gates are recorded in [the evidence](docs/evidence/phase-3/cashflow-corrections.md).

**E3-13 / P3-02–P3-06 Phase 3 closure, 9 September 2026:** Applied the forward reconciliation/document-snapshot migration, refreshed generated types, and verified exact cashflow behavior with the existing rollback-only SQL check. Two-session probes demonstrated invoice-row and staff-counter lock serialization; all probes rolled back and reconciliation/residue checks passed. Live authenticated localhost inspection covered financial settings, invoice, receipt and voucher documents on desktop/mobile; a focused Chromium capture rendered the voucher as a single-page A5 PDF without clipping. Security/performance advisors were reviewed. `pnpm format`, changed-file React Doctor (90/100; one accepted complexity warning), `pnpm lint`, `pnpm typecheck`, and `pnpm build` passed; no broad E2E suite was added or run. Full evidence: [Phase 3 closure verification](docs/evidence/phase-3/phase-3-closure-verification.md).

- **Prior active scope (9 September 2026):** Phase 3 was verified complete. Superseded by E4-01 above.
- **Approved cashflow behavior:** School Fees uses live invoice-number/student-name search. Feeding and admission use daily aggregates without student selection. Miscellaneous income uses a required income name. Other expenses require a name, stored in the existing description field. Daily aggregates are recorded once per date/payment method; correction uses reversal followed by replacement.
- **Implementation and verification:** See [cashflow correction evidence](docs/evidence/phase-3/cashflow-corrections.md) and [Phase 3 closure evidence](docs/evidence/phase-3/phase-3-closure-verification.md). The final migration adds immutable reprint snapshots and persisted reversal references without changing transactional RPC signatures. Focused rollback-only SQL, two-session concurrency, reconciliation, permissions, browser and print/PDF checks passed.
- **Prior next work:** Phase 4 authorization was subsequently supplied and completed in E4-01.

### Historical handover (3 September 2026; superseded where noted above)

- **Gate:** Phase 1/2 closure is preserved. STAFF-01 has applied compatibility support and 18 verified staff records, but assignment decisions (nursery aliases, Term 1 effective dates, head/class teachers) and overlapping concurrency evidence remain open — these need real Chief Engineer facts and were not guessed. The Chief Engineer authorized starting P3-01 in parallel with those open STAFF-01 items (D-10, E3-02); STAFF-01 itself must still close before Phase 3 is considered complete. **P3-01 is verified complete** (E3-02–E3-05): `fee_components`/`fee_component_rates` (now including `feeding_fee`/`admission_fee` as flat-scope rows, E3-06)/`payment_methods`/`expense_categories`/`misc_income_categories` (seeded with 2026/2027 Term 1 rates), `invoices`/`invoice_lines` plus `private.allocate_document_number`, `payments`/`receipts` plus `private.finance_requests`, and `feeding_receipts`/`admission_receipts`/`misc_receipts`/`expenses`. Every finance ledger table uses closed direct writes (RLS, `SELECT`-only) except through SECURITY DEFINER RPCs; the settings tables (fee rates/categories/methods) use direct writes gated by `finance.settings.manage` (SUPER_ADMIN-only). **P3-02 is in progress**: `/settings/financials` (E3-06) lets an authorized administrator edit current-term base class fees, transport charges, feeding/admission amounts, and manage payment methods/expense categories/misc-income categories, plus a read-only numbering-format reference; live authenticated browser/design screenshots and `react-doctor` were not run (no session/tool available). **P3-03 is in progress** (E3-07): `generate_term_invoices` (bulk + single-student, per D-10) and `cancel_invoice` are applied and verified end to end against real fee rates (a JHS 1/Anomaa Kojo invoice correctly totalled GHS 1,832.00), including duplicate-skip, bulk-skip, cancel-then-regenerate, double-cancel rejection, and anonymous denial — all via rolled-back transactions, no residual test data. New permission `finance.transactions.manage` (SUPER_ADMIN/ADMINISTRATOR/ACCOUNTANT). Invoice list/detail views are now built and verified (E3-08): `/financials/invoices` (search/status filter, pagination, bulk/single generation panel) and `/financials/invoices/[id]` (printable A4-proportioned invoice with a `VOID` banner and cancellation form). The remaining P3-03 sub-step is the compact A5 receipt/expense-voucher document template, deferred until P3-04/P3-05 create real payments/expenses to render; the student finance tab remains locked until those RPCs and views exist too. P3-04/P3-05 RPCs and Phase 4/5 work were not started.
- **Staff review:** Private ignored .tools/staff-onboarding/roster-review.md and .json preserve source names, subjects and proposed class links. All 18 names/IDs/types/positions/subjects match the database; IDs 001-018 are allocated. Unknown name components, contacts and employment dates remain null. The generated workbook is incompatible with ExcelJS and must not be re-imported; onboarding used authenticated forms instead. Keep real names out of tracked fixtures, migrations and evidence. Fresh clones require an approved private source.
- **Applied state:** Seventeen migrations match remote history; latest is 20260902185750_staff_known_details_and_teaching_assignments. Types regenerated via MCP. Current counts: 18 active staff (14 teaching/four non-teaching), zero assignments, 18 requests, 18 staff audit entries, next display number 019. Test-only project remains cefwopisbgfctzdloequ. The existing Super Administrator is signed in; do not claim zero sessions or a pending password change from historical evidence. No staff login accounts were created. Prior school/role/storage configuration is unchanged.
- **School decisions:** `2026/2027` is current. Term 1 starts 8 September 2026; 7 December is a clearly disclosed, revisable three-month planning end. Terms 2/3 are unscheduled. The supplied fee file/image agree and are captured as Phase 3 requirements, not database fee configuration. See [school configuration reference](docs/requirements/school-configuration-2026-2027.md).
- **Finance structure:** [Financial Structure.md](Project%20Files/Financial%20Structure.md) records the current school-fee matrix, GHS 10 feeding fee, GHS 50 admission fee, miscellaneous income, notebook-informed daily/weekly cashflow, quick daily expenses, monthly salary deductions, official invoice/receipt/voucher requirements, configurable D-04 proposals, accountability controls, and remaining beta decisions. No financial schema or records have been created.
- **Verified behavior:** Student, staff, and administrator reads plus private student photos are RLS-protected; direct people/admin-table writes are denied; create/import/profile writes are permission-gated. Staff records remain separate from Auth. Administrator account creation uses a temporary password without OTP/email invitation, and the application blocks protected routes while `must_change_password` is true. Administrator mutations require a valid session and fresh SUPER_ADMIN permission; self-management and loss of the final active Super Administrator remain blocked. Finance sections remain explicitly unavailable.
- **Auth redesign:** Authentication is email-and-password only: no OTP, invitation link, or MFA step. A Super Administrator creates each administrator with an issued temporary password; first sign-in is forced to the dedicated password-replacement route before any protected workflow is available. Credential-bearing login/change-password requests use size/origin-checked Route Handlers instead of Server Actions so passwords are not serialized into Next action traces. Replacement passwords require at least eight characters plus uppercase, lowercase, number and symbol, must differ from the temporary password, and are capped at 128 characters. The responsive editorial auth shell uses the modern African classroom image, transparent official crest and reduced-motion-aware Framer Motion entrances.
- **Operating profile:** 10 devices, about 200 students, Chrome/Edge, desktop-first responsive UI, 5–10 concurrent users, and 5,000–10,000 finance records per academic term. Exact school network speed is not measured; validate it at P5/P6 rather than blocking feature delivery.
- **Verification tooling (3 Sep 2026):** The hosted `pnpm test:e2e` gate failed intermittently on tablet/mobile: axe scanned the login page during the Framer Motion entrance fade, so mid-animation blended colors tripped color-contrast even though every token passes WCAG AA at rest (`--primary` 5.47:1, `--muted-foreground` 4.98:1 on white). Fixed by emulating `prefers-reduced-motion: reduce` through Playwright `contextOptions` in `playwright.config.ts`, which the auth shell already honours. Full synthetic suite verified 24/24 locally against a CI-matching production build; the login accessibility scan passed three consecutive repeats on desktop, tablet and mobile.
- **Residuals:** Security advisors report fourteen intentional authenticated SECURITY DEFINER RPC warnings, two INFO no-policy notices for new private deny-all tables, and existing Free-plan leaked-password protection ([remediation](https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection)). Performance has sixteen unused-index INFO notices and no warnings/errors. React Doctor reports six non-blocking findings; see E3-00C triage. Overlapping concurrency and live spreadsheet-import roundtrip remain unverified; no broad E2E suite was run.
- **Lifecycle:** D-01 permits this project for testing only. The three synthetic Auth users, all identified test people data, and the ignored `.env.test.local` credential file are removed; historical live-provider suites require newly authorized fixtures before they can run again. Before live data, disable the test-target manifest/CI dispatch, preserve schema/role/configuration history, and satisfy D-05/D-07 backup, approved origins, monitoring, password-policy acceptance, and release approval. Clearing arbitrary tables remains prohibited.
- **Artifacts/runtime:** [Administrator evidence](docs/evidence/phase-2/administrator-management-verification.md), [Staff evidence](docs/evidence/phase-2/staff-management-verification.md), [Student directory evidence](docs/evidence/phase-2/student-directory-verification.md), [Phase 1 evidence](docs/evidence/phase-1/configuration-verification.md), [Phase 0 local verification](docs/evidence/phase-0/verification.md), and [Phase 0 backend evidence](docs/evidence/phase-0/backend-verification.md). Node 24.20.0 and pnpm 10.33.0 are pinned. Secrets and generated output remain ignored; only the test-target Edge Function was deployed, with no production deployment.
- **Next agent:** Read startup sources, staff requirements and E3-00C evidence. MCP is connected; do not repeat OAuth or re-import the 18 staff. Confirm assignment period/start date and nursery aliases, add verified class-subject pairs through the authenticated app, and leave unspecified leadership/specialist classes unset. Resolve concurrency verification without bypassing MCP or impersonating an existing identity. Finish STAFF-01 before P3-01 only; no P3-02 or later. Preserve assignment history and separate people from login provisioning.
